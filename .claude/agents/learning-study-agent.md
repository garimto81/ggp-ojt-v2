# Learning Study Agent Rules

**Version**: 1.1.0 | **Domain**: Learning Study | **Level**: 1

---

## Identity

| 속성 | 값 |
|------|-----|
| **Role** | 학습 진행 전문가 |
| **Scope** | `features/learning/study/` (Vertical Slicing 후) |
| **Managed Blocks** | `learning.roadmap`, `learning.section` |

---

## Block Responsibilities

### learning.roadmap

| 항목 | 내용 |
|------|------|
| **책임** | 학습 로드맵 표시, 진도 현황 |
| **입력** | `{ userId, team? }` |
| **출력** | `{ docs[], progress }` |
| **현재 위치** | `MenteeList.jsx` |

### learning.section

| 항목 | 내용 |
|------|------|
| **책임** | 섹션 순차 학습, 네비게이션 |
| **입력** | `{ docId, sectionIndex }` |
| **출력** | `SectionState` |
| **현재 위치** | `MenteeStudy.jsx` (1-148행, 319-428행) |

---

## Dependencies

### Internal

```javascript
import { useDocs } from '@/features/content/manage';
import { useAuth } from '@/features/auth';
import { useToast } from '@/shared/contexts/ToastContext';
```

### External

없음 (순수 UI/상태 처리)

### Cross-Domain

- `auth-agent`: 사용자 인증 확인 (`user.id`)
- `content-manage-agent`: 문서 데이터 조회 (읽기 전용)
- `learning-quiz-agent`: 학습 완료 후 퀴즈 시작 전달

---

## Constraints

### DO

- ✅ 인증된 사용자만 학습 진행
- ✅ 섹션 순차 표시 (이전/다음 네비게이션)
- ✅ 학습 진도 시각적 표시 (프로그레스 바)
- ✅ 원문 링크 제공 (`source_url`)

### DON'T

- ❌ 문서 데이터 직접 수정 (content-manage-agent 담당)
- ❌ 퀴즈 로직 처리 (learning-quiz-agent 담당)
- ❌ 학습 기록 저장 (learning-quiz-agent 담당)
- ❌ 다른 사용자의 진도 접근

---

## Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                 LEARNING STUDY FLOW                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. ROADMAP (MenteeList)                                     │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  사용자 로그인 확인                                  │    │
│  │       │                                             │    │
│  │       ▼                                             │    │
│  │  fetchDocs({ team }) → ojt_docs 조회               │    │
│  │       │                                             │    │
│  │       ▼                                             │    │
│  │  learning_progress 조회 (user_id 기준)              │    │
│  │       │                                             │    │
│  │       ▼                                             │    │
│  │  로드맵 UI 렌더링 (완료/진행중/미시작)              │    │
│  │       │                                             │    │
│  │       ▼                                             │    │
│  │  문서 클릭 → learning.section으로 전달             │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  2. SECTION (MenteeStudy - 학습 모드)                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  selectedDoc.sections 로드                          │    │
│  │       │                                             │    │
│  │       ▼                                             │    │
│  │  currentSection 상태 관리                           │    │
│  │       │                                             │    │
│  │       ├── 이전 버튼 → currentSection - 1           │    │
│  │       │                                             │    │
│  │       └── 다음 버튼 → currentSection + 1           │    │
│  │                  │                                  │    │
│  │                  ▼                                  │    │
│  │  마지막 섹션 완료 → studyCompleted = true          │    │
│  │                  │                                  │    │
│  │                  ▼                                  │    │
│  │  [퀴즈 시작] → learning-quiz-agent로 전달          │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## UI Components

### 로드맵 표시

```
┌─────────────────────────────────────────┐
│  Team: Development                       │
├─────────────────────────────────────────┤
│  [✓] Step 1: 개발 환경 설정  (완료)      │
│  [◐] Step 2: Git 사용법      (진행중)    │
│  [ ] Step 3: 코드 리뷰       (미시작)    │
└─────────────────────────────────────────┘
```

### 섹션 학습

```
┌─────────────────────────────────────────┐
│  ← 목록으로                 Step 2/3    │
├─────────────────────────────────────────┤
│  Git 사용법                             │
│  예상 학습 시간: 15분                   │
│  [🔗 원문 보기]                         │
│                                         │
│  섹션 2/5 ████████░░░░ 40%              │
├─────────────────────────────────────────┤
│  ## 브랜치 관리                         │
│                                         │
│  Git 브랜치는...                        │
│                                         │
├─────────────────────────────────────────┤
│  [← 이전 섹션]      [다음 섹션 →]       │
└─────────────────────────────────────────┘
```

---

## Error Codes

| Code | 의미 | 처리 |
|------|------|------|
| `STUDY_NOT_AUTHENTICATED` | 비로그인 상태 | 로그인 유도 |
| `STUDY_DOC_NOT_FOUND` | 문서 없음 | 목록으로 이동 |
| `STUDY_NO_SECTIONS` | 섹션 없음 | 안내 메시지 |

---

## Testing Guidelines

### Unit Tests

```javascript
describe('learning.roadmap', () => {
  it('should fetch docs filtered by team', async () => {});
  it('should show progress for authenticated user', async () => {});
});

describe('learning.section', () => {
  it('should navigate between sections', () => {});
  it('should mark study completed on last section', () => {});
});
```

---

## Related Files (현재 → 리팩토링 후)

| 현재 파일 | 리팩토링 후 |
|----------|------------|
| `MenteeList.jsx` | `features/learning/study/components/MenteeList.jsx` |
| `MenteeStudy.jsx` (학습 모드) | `features/learning/study/components/SectionViewer.jsx` |
| - | `features/learning/study/hooks/useLearningProgress.js` |
