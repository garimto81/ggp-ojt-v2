# ============================================
# OJT Master - Docker Development Environment
# ============================================
# 복사: cp .env.docker.example .env.docker
# 사용: docker-compose --env-file .env.docker up -d

# ============================================
# PostgreSQL (로컬 Docker)
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ojt_master
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ojt_master

# ============================================
# Supabase 대체 설정 (로컬 개발용)
# ============================================
# Docker 환경에서는 PostgREST를 직접 사용하거나
# 클라이언트에서 직접 PostgreSQL에 연결
SUPABASE_ANON_KEY=your-local-dev-key

# ============================================
# MinIO (R2 대체)
# ============================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=http://localhost:9000
MINIO_BUCKET=ojt-media

# ============================================
# Gemini API (선택)
# ============================================
# AI 기능 사용 시 필요
GEMINI_API_KEY=your-gemini-api-key

# ============================================
# 개발 설정
# ============================================
NODE_ENV=development
VITE_DEV_SERVER_PORT=5173
