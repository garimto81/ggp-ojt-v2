# OJT Master v2.13.0 - Environment Variables

# Supabase (Required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Cloudflare R2 Worker (Optional - for image upload)
VITE_R2_WORKER_URL=https://ojt-r2-upload.your-worker.workers.dev

# Local AI Server (Optional - for internal LLM server)
# 사내 MLC LLM 서버 URL (OpenAI 호환 API)
# 설정 시 1순위로 사용, 미설정 시 WebLLM fallback
VITE_LOCAL_AI_URL=http://192.168.1.100:8000

# Note:
# - Local AI 서버가 설정되면 1순위로 사용
# - Local AI 실패 시 WebLLM (브라우저 내 LLM) fallback
# - 둘 다 실패 시 원문 그대로 저장 (Fallback Content)
