# OJT Master v2.14.0 - Environment Variables (Local-Only Architecture)

# ============================================
# Local Network Configuration (Issue #114)
# ============================================
# Base API URL for local PostgreSQL REST endpoint
# Default: Local network server
VITE_API_URL=http://10.10.100.209

# ============================================
# Database (Temporary - Supabase Client)
# ============================================
# TODO (Issue #114): Replace with direct REST API calls
# Current: Supabase JS client for compatibility
# Target: Direct fetch() to VITE_API_URL/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
# VITE_SUPABASE_ANON_KEY=your-anon-key  # 레거시 (deprecated)

# ============================================
# Local AI Server (Priority)
# ============================================
# 사내 AI 서버 URL (vLLM with Qwen/Qwen3-4B)
# This is the PRIMARY AI engine for content generation
VITE_LOCAL_AI_URL=http://10.10.100.209:8001

# ============================================
# Authentication Mode (Issue #105)
# ============================================
# 인증 모드 선택:
# - email: Email 인증만 (Docker 사내 배포 권장)
# - google: Google OAuth만 (OAuth 가능한 환경)
# - hybrid: 둘 다 지원 (기본값)
VITE_AUTH_MODE=email

# ============================================
# Optional: Cloud Services (Not required)
# ============================================
# Cloudflare R2 for image uploads (optional, deprecated)
# Use local file storage or MinIO instead
# VITE_R2_WORKER_URL=https://ojt-r2-upload.your-worker.workers.dev

# ============================================
# Notes
# ============================================
# Local-Only Architecture Benefits:
# - No cloud dependencies (Vercel, Supabase, Cloudflare)
# - All data stays in local network
# - Simplified deployment (Docker on-premise)
# - Lower operational cost
#
# WebLLM Fallback:
# - If VITE_LOCAL_AI_URL is not set, WebLLM (browser-based) will be used
# - First run downloads ~2.4GB model (Qwen 2.5 3B)
# - Fully offline after initial download
