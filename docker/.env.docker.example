# OJT Master - Docker Environment Variables
# 이 파일을 .env.docker로 복사하여 사용하세요.
#
# 사용법:
#   cp .env.docker.example .env.docker
#   # 값 수정 후
#   docker-compose --env-file .env.docker up -d

# ========================================
# Frontend 환경 변수 (빌드 시 적용)
# ========================================

# Local AI API URL (Same-Origin 프록시 사용)
# Docker 환경에서는 nginx 프록시를 통해 접근
VITE_LOCAL_AI_URL=/api

# Supabase 설정
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# R2 Worker URL (선택)
VITE_R2_WORKER_URL=https://ojt-r2-upload.your-worker.workers.dev

# ========================================
# vLLM 설정
# ========================================

# 사용할 AI 모델
VLLM_MODEL=Qwen/Qwen3-4B

# GPU 메모리 사용률 (0.0 ~ 1.0)
# 다른 작업과 GPU 공유 시 낮추세요
VLLM_GPU_MEMORY_UTILIZATION=0.8

# 최대 컨텍스트 길이
VLLM_MAX_MODEL_LEN=8192

# ========================================
# SSL 인증서 경로
# ========================================

# 자체 서명 인증서 생성:
#   openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#     -keyout ssl/key.pem -out ssl/cert.pem \
#     -subj "/CN=localhost"
#
# Let's Encrypt 사용 시:
#   certbot certonly --standalone -d your-domain.com
#   cp /etc/letsencrypt/live/your-domain.com/fullchain.pem ssl/cert.pem
#   cp /etc/letsencrypt/live/your-domain.com/privkey.pem ssl/key.pem
