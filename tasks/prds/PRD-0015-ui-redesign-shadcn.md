# PRD-0015: UI 디자인 개선 (shadcn/ui)

| 항목 | 내용 |
|------|------|
| **PRD ID** | PRD-0015 |
| **제목** | OJT Master UI 디자인 개선 |
| **상태** | Draft |
| **작성일** | 2025-12-12 |
| **관련 이슈** | #228 |
| **기술 스택** | React 19 + Tailwind CSS 4 + shadcn/ui |

---

## 1. 개요

### 1.1 배경

현재 OJT Master는 기본 Tailwind CSS 스타일만 적용되어 있어 시각적 일관성과 현대적인 느낌이 부족합니다. PRD-0014에서 톤앤매너 가이드라인과 디자인 토큰을 정의했으나, 실제 컴포넌트에 체계적으로 적용되지 않았습니다.

### 1.2 목표

- **일관성**: 모든 화면에서 통일된 디자인 언어 사용
- **현대성**: 2025년 트렌드에 맞는 세련된 UI
- **접근성**: WCAG 2.1 AA 기준 충족
- **유지보수성**: 컴포넌트 기반 설계로 쉬운 유지보수

### 1.3 솔루션

**shadcn/ui (canary)** 도입
- React 19 + Tailwind CSS v4 완벽 호환
- 코드 복사 방식으로 완전한 커스터마이징 가능
- Radix UI 기반 접근성 내장

---

## 2. 디자인 시스템

### 2.1 색상 팔레트 (기존 @theme 확장)

```css
@theme {
  /* Primary - 브랜드 컬러 */
  --color-primary-50: oklch(0.97 0.02 250);
  --color-primary-100: oklch(0.93 0.04 250);
  --color-primary-500: oklch(0.55 0.18 250);
  --color-primary-600: oklch(0.48 0.20 250);
  --color-primary-700: oklch(0.40 0.18 250);

  /* Semantic Colors */
  --color-success: oklch(0.72 0.19 142);
  --color-warning: oklch(0.80 0.15 85);
  --color-error: oklch(0.65 0.20 25);
  --color-info: oklch(0.70 0.15 230);

  /* Neutral */
  --color-background: oklch(0.985 0.002 250);
  --color-foreground: oklch(0.15 0.02 250);
  --color-muted: oklch(0.95 0.01 250);
  --color-border: oklch(0.90 0.01 250);
}
```

### 2.2 타이포그래피

```css
@theme {
  --font-sans: 'Pretendard Variable', system-ui, sans-serif;

  /* Font Sizes */
  --text-xs: 0.75rem;    /* 12px */
  --text-sm: 0.875rem;   /* 14px */
  --text-base: 1rem;     /* 16px */
  --text-lg: 1.125rem;   /* 18px */
  --text-xl: 1.25rem;    /* 20px */
  --text-2xl: 1.5rem;    /* 24px */
  --text-3xl: 1.875rem;  /* 30px */
}
```

### 2.3 간격 & 라운딩

```css
@theme {
  /* Border Radius */
  --radius-sm: 0.375rem;  /* 6px */
  --radius-md: 0.5rem;    /* 8px */
  --radius-lg: 0.75rem;   /* 12px */
  --radius-xl: 1rem;      /* 16px */

  /* Shadows */
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
}
```

---

## 3. 화면별 목업 디자인

### 3.1 전체 레이아웃 구조

```mermaid
graph TB
    subgraph App["OJT Master App"]
        Header["🔷 Header (고정)"]
        Main["📄 Main Content"]
    end

    Header --> Logo["로고 + 버전"]
    Header --> Nav["AI 상태 | 모드 | 사용자"]

    Main --> Admin["Admin Dashboard"]
    Main --> Mentor["Mentor Dashboard"]
    Main --> Mentee["Mentee View"]
```

### 3.2 Header 디자인

```mermaid
graph LR
    subgraph Header["Header - h-16 bg-white border-b"]
        subgraph Left["좌측"]
            Logo["🔷 OJT<br/>로고"]
            Title["OJT Master<br/>v2.31.0"]
        end

        subgraph Right["우측"]
            AI["🟢 Gemini<br/>120ms"]
            Mode["모드 ▼"]
            User["김관리<br/>Admin | 개발팀"]
            Logout["로그아웃"]
        end
    end

    Logo --> Title
    AI --> Mode --> User --> Logout
```

**shadcn/ui 컴포넌트**:
- `Button` (variant: ghost, outline)
- `DropdownMenu` (모드 전환)
- `Badge` (역할, 부서)
- `Avatar` (사용자)

### 3.3 Admin Dashboard

```mermaid
graph TB
    subgraph Dashboard["Admin Dashboard"]
        subgraph Stats["📊 통계 카드 (Grid 4열)"]
            S1["👥 총 사용자<br/>156명"]
            S2["📄 총 문서<br/>42개"]
            S3["📚 학습 기록<br/>1,234건"]
            S4["✅ 통과율<br/>87%"]
        end

        subgraph Tabs["🗂️ 탭 네비게이션"]
            T1["사용자 관리"]
            T2["콘텐츠 관리"]
            T3["통계"]
            T4["설정"]
        end

        subgraph Content["📋 탭 콘텐츠"]
            Table["데이터 테이블"]
        end
    end

    Stats --> Tabs --> Content
```

**통계 카드 상세 디자인**:

```mermaid
graph TB
    subgraph Card["Card Component"]
        subgraph CardHeader["CardHeader"]
            Icon["👥"]
            Label["총 사용자"]
        end
        subgraph CardContent["CardContent"]
            Value["156"]
            Unit["명"]
        end
        subgraph CardFooter["CardFooter (선택)"]
            Trend["↑ 12% 지난주 대비"]
        end
    end
```

### 3.4 데이터 테이블 디자인

```mermaid
graph TB
    subgraph DataTable["📋 사용자 관리 테이블"]
        subgraph Toolbar["Toolbar"]
            Search["🔍 검색..."]
            Filter1["역할 ▼"]
            Filter2["부서 ▼"]
            PerPage["20개씩 ▼"]
        end

        subgraph TableHeader["Table Header"]
            H1["☑️"]
            H2["이름 ↕"]
            H3["역할"]
            H4["부서"]
            H5["가입일 ↕"]
            H6["액션"]
        end

        subgraph TableRow["Table Row (hover:bg-muted)"]
            R1["☐"]
            R2["김신입"]
            R3["🟢 Mentee"]
            R4["개발팀"]
            R5["2025-12-01"]
            R6["⚙️ ▼"]
        end

        subgraph Pagination["Pagination"]
            Prev["← 이전"]
            Pages["1 2 3 ... 10"]
            Next["다음 →"]
        end
    end

    Toolbar --> TableHeader --> TableRow --> Pagination
```

**shadcn/ui 컴포넌트**:
- `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableCell`
- `Input` (검색)
- `Select` (필터)
- `Checkbox` (선택)
- `DropdownMenu` (액션)
- `Pagination`

### 3.5 Mentor Dashboard (콘텐츠 생성)

```mermaid
graph TB
    subgraph MentorDash["Mentor Dashboard"]
        subgraph Header["페이지 헤더"]
            Title["📝 새 학습 자료 만들기"]
            Subtitle["AI가 콘텐츠를 분석하고 퀴즈를 생성합니다"]
        end

        subgraph Form["콘텐츠 입력 폼"]
            subgraph InputType["입력 방식 선택"]
                Tab1["📄 텍스트"]
                Tab2["🔗 URL"]
                Tab3["📁 PDF"]
            end

            subgraph InputArea["입력 영역"]
                Textarea["내용을 입력하세요..."]
            end

            subgraph Options["옵션"]
                Toggle["🤖 AI 분석 건너뛰기"]
                Team["팀 선택 ▼"]
                Step["단계 선택 ▼"]
            end
        end

        subgraph Actions["액션 버튼"]
            Cancel["취소"]
            Save["저장하기"]
        end
    end

    Header --> Form --> Actions
```

### 3.6 Mentee 학습 뷰

```mermaid
graph TB
    subgraph MenteeView["Mentee 학습 화면"]
        subgraph DocList["📚 학습 자료 목록"]
            subgraph DocCard1["Card - 완료"]
                DC1_Icon["✅"]
                DC1_Title["React 기초"]
                DC1_Meta["개발팀 | Step 1"]
                DC1_Progress["100%"]
            end

            subgraph DocCard2["Card - 진행중"]
                DC2_Icon["📖"]
                DC2_Title["TypeScript 입문"]
                DC2_Meta["개발팀 | Step 2"]
                DC2_Progress["60%"]
            end

            subgraph DocCard3["Card - 미시작"]
                DC3_Icon["📄"]
                DC3_Title["테스트 작성법"]
                DC3_Meta["개발팀 | Step 3"]
                DC3_Progress["0%"]
            end
        end

        subgraph StudyView["📖 학습 뷰어"]
            Content["섹션 콘텐츠"]
            Navigation["이전 | 다음"]
            Quiz["퀴즈 풀기 →"]
        end
    end

    DocList --> StudyView
```

### 3.7 퀴즈 화면

```mermaid
graph TB
    subgraph QuizView["🎯 퀴즈 화면"]
        subgraph Progress["진행 상태"]
            ProgressBar["████████░░ 8/10"]
        end

        subgraph Question["문제"]
            QNum["Q8"]
            QText["React에서 상태 관리를 위해<br/>사용하는 Hook은?"]
        end

        subgraph Options["보기 (RadioGroup)"]
            O1["○ useState"]
            O2["○ useEffect"]
            O3["○ useContext"]
            O4["○ useRef"]
        end

        subgraph Actions["액션"]
            Skip["건너뛰기"]
            Submit["제출하기"]
        end
    end

    Progress --> Question --> Options --> Actions
```

### 3.8 로그인 / 역할 선택

```mermaid
graph TB
    subgraph LoginPage["🔐 역할 선택 화면"]
        subgraph Logo["로고 영역"]
            LogoIcon["🔷 OJT Master"]
            Tagline["AI 기반 신입사원 온보딩"]
        end

        subgraph RoleCards["역할 선택 카드"]
            subgraph AdminCard["Admin Card"]
                A_Icon["👑"]
                A_Title["관리자"]
                A_Desc["사용자 및 콘텐츠 관리"]
            end

            subgraph MentorCard["Mentor Card"]
                M_Icon["📚"]
                M_Title["멘토"]
                M_Desc["학습 자료 생성"]
            end

            subgraph MenteeCard["Mentee Card"]
                ME_Icon["🎓"]
                ME_Title["멘티"]
                ME_Desc["학습 및 퀴즈"]
            end
        end

        subgraph Form["로그인 폼"]
            NameInput["이름 입력"]
            LoginBtn["시작하기 →"]
        end
    end

    Logo --> RoleCards --> Form
```

---

## 4. 컴포넌트 명세

### 4.1 도입할 shadcn/ui 컴포넌트

| 컴포넌트 | 용도 | 우선순위 |
|----------|------|----------|
| `Button` | 모든 버튼 (Primary, Secondary, Ghost, Destructive) | P0 |
| `Card` | 통계 카드, 문서 카드, 역할 선택 카드 | P0 |
| `Table` | 사용자 관리, 콘텐츠 관리 테이블 | P0 |
| `Input` | 검색, 폼 입력 | P0 |
| `Select` | 필터, 드롭다운 | P0 |
| `Badge` | 역할, 부서, 상태 표시 | P0 |
| `Dialog` | 모달, 확인 다이얼로그 | P1 |
| `DropdownMenu` | 액션 메뉴, 모드 전환 | P1 |
| `Tabs` | 대시보드 탭 | P1 |
| `Progress` | 학습 진행률, 퀴즈 진행 | P1 |
| `RadioGroup` | 퀴즈 보기 선택 | P1 |
| `Checkbox` | 테이블 선택, 옵션 토글 | P1 |
| `Avatar` | 사용자 프로필 | P2 |
| `Skeleton` | 로딩 상태 | P2 |
| `Toast` | (기존 유지, sonner로 마이그레이션 고려) | P2 |

### 4.2 커스텀 컴포넌트

```
src/
└── components/
    └── ui/           # shadcn/ui 복사본
        ├── button.jsx
        ├── card.jsx
        ├── table.jsx
        ├── input.jsx
        ├── select.jsx
        ├── badge.jsx
        ├── dialog.jsx
        ├── dropdown-menu.jsx
        ├── tabs.jsx
        ├── progress.jsx
        └── ...
```

---

## 5. 구현 계획

### Phase 1: 기반 구축 (Day 1-2)

```mermaid
gantt
    title Phase 1: 기반 구축
    dateFormat  YYYY-MM-DD
    section Setup
    shadcn/ui 초기화           :a1, 2025-12-13, 1d
    디자인 토큰 통합            :a2, after a1, 1d
    section Core Components
    Button 컴포넌트            :a3, after a1, 1d
    Card 컴포넌트              :a4, after a1, 1d
    Input/Select              :a5, after a3, 1d
```

**작업 내용**:
1. `npx shadcn@canary init` 실행
2. 기존 `@theme` 색상과 shadcn 테마 통합
3. Button, Card, Input, Select 컴포넌트 추가

### Phase 2: 테이블 & 폼 (Day 3-4)

```mermaid
gantt
    title Phase 2: 테이블 & 폼
    dateFormat  YYYY-MM-DD
    section Table
    Table 컴포넌트             :b1, 2025-12-15, 1d
    Admin 테이블 적용          :b2, after b1, 1d
    section Form
    Dialog 컴포넌트            :b3, 2025-12-15, 1d
    폼 컴포넌트 적용           :b4, after b3, 1d
```

**작업 내용**:
1. Table, Pagination 컴포넌트 추가
2. AdminDashboard 사용자 테이블 리디자인
3. Dialog, Tabs 컴포넌트 추가

### Phase 3: 대시보드 리디자인 (Day 5-6)

```mermaid
gantt
    title Phase 3: 대시보드 리디자인
    dateFormat  YYYY-MM-DD
    section Header
    Header 리디자인            :c1, 2025-12-17, 1d
    section Dashboard
    Admin Dashboard           :c2, after c1, 1d
    Mentor Dashboard          :c3, after c2, 1d
```

### Phase 4: 학습 뷰 & 퀴즈 (Day 7-8)

```mermaid
gantt
    title Phase 4: 학습 뷰 & 퀴즈
    dateFormat  YYYY-MM-DD
    section Mentee
    MenteeList 리디자인        :d1, 2025-12-19, 1d
    MenteeStudy 리디자인       :d2, after d1, 1d
    section Quiz
    QuizSession 리디자인       :d3, after d2, 1d
```

### Phase 5: 마무리 & QA (Day 9-10)

```mermaid
gantt
    title Phase 5: 마무리 & QA
    dateFormat  YYYY-MM-DD
    section Polish
    로그인 페이지 리디자인      :e1, 2025-12-21, 1d
    반응형 점검               :e2, after e1, 1d
    section QA
    E2E 테스트               :e3, after e2, 1d
    접근성 검증               :e4, after e3, 1d
```

---

## 6. 비주얼 비교

### 6.1 Before (현재)

```
┌─────────────────────────────────────────────────┐
│ [OJT] OJT Master                    🟢 Gemini   │
│       v2.31.0                 모드 | 김관리 | 로그아웃 │
├─────────────────────────────────────────────────┤
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                │
│ │ 156 │ │  42 │ │1234 │ │ 87% │                │
│ │사용자│ │ 문서 │ │기록 │ │통과율│                │
│ └─────┘ └─────┘ └─────┘ └─────┘                │
│                                                 │
│ [사용자관리] [콘텐츠] [통계] [설정]               │
│ ─────────────────────────────────               │
│ 이름검색... [역할▼] [부서▼]                      │
│ ┌─────────────────────────────────┐            │
│ │ 이름    │ 역할  │ 부서  │ 액션  │            │
│ │─────────┼───────┼───────┼───────│            │
│ │ 김신입  │mentor▼│개발팀▼│ 정지  │            │
│ └─────────────────────────────────┘            │
└─────────────────────────────────────────────────┘
```

### 6.2 After (shadcn/ui 적용)

```
┌─────────────────────────────────────────────────┐
│ ╔═══╗                                           │
│ ║OJT║ OJT Master          🟢 Gemini 120ms      │
│ ╚═══╝ v2.31.0              [모드▼] 👤 김관리    │
│                            Admin | 개발팀 [로그아웃]│
├─────────────────────────────────────────────────┤
│                                                 │
│  ╭─────────╮ ╭─────────╮ ╭─────────╮ ╭─────────╮│
│  │ 👥      │ │ 📄      │ │ 📚      │ │ ✅      ││
│  │   156   │ │    42   │ │  1,234  │ │   87%   ││
│  │ 총 사용자│ │ 총 문서  │ │ 학습 기록│ │ 통과율  ││
│  │ ↑12%    │ │ ↑3개    │ │ ↑156건  │ │ ↑5%p   ││
│  ╰─────────╯ ╰─────────╯ ╰─────────╯ ╰─────────╯│
│                                                 │
│  ╭─────────────────────────────────────────────╮│
│  │ [사용자 관리] [콘텐츠 관리] [통계] [설정]    ││
│  ├─────────────────────────────────────────────┤│
│  │ 🔍 이름 검색...  [모든 역할▼] [모든 부서▼]  ││
│  │                                             ││
│  │ ☐ │ 이름 ↕   │ 역할      │ 부서   │ 액션   ││
│  │───┼──────────┼───────────┼────────┼────────││
│  │ ☐ │ 김신입   │ 🟢 Mentee │ 개발팀 │ ⋯ ▼   ││
│  │ ☐ │ 이멘토   │ 🔵 Mentor │ 기획팀 │ ⋯ ▼   ││
│  │ ☐ │ 박관리   │ 🟣 Admin  │ 운영팀 │ ⋯ ▼   ││
│  │                                             ││
│  │           ← 이전  1 2 3 ... 10  다음 →      ││
│  ╰─────────────────────────────────────────────╯│
└─────────────────────────────────────────────────┘
```

---

## 7. 성공 지표

| 지표 | 현재 | 목표 |
|------|------|------|
| Lighthouse Performance | - | 90+ |
| Lighthouse Accessibility | - | 95+ |
| 컴포넌트 일관성 | 낮음 | 높음 |
| 개발자 생산성 | - | 컴포넌트 재사용으로 30% 향상 |

---

## 8. 리스크 & 대응

| 리스크 | 확률 | 영향 | 대응 |
|--------|------|------|------|
| shadcn/ui canary 불안정 | 중 | 중 | 안정 버전 출시 대기 또는 특정 커밋 고정 |
| 기존 스타일 충돌 | 높음 | 낮음 | CSS 레이어 분리, 점진적 마이그레이션 |
| 번들 사이즈 증가 | 낮음 | 낮음 | 필요한 컴포넌트만 선택적 추가 |

---

## 9. 참고 자료

- [shadcn/ui 공식 문서](https://ui.shadcn.com/)
- [Tailwind CSS v4 문서](https://tailwindcss.com/docs)
- [PRD-0014 톤앤매너 가이드라인](./PRD-0014-tone-and-manner.md)
- [Issue #228](https://github.com/garimto81/ggp-ojt-v2/issues/228)

---

## 10. 승인

| 역할 | 이름 | 날짜 | 서명 |
|------|------|------|------|
| PM | - | - | - |
| Design | - | - | - |
| Dev Lead | - | - | - |
