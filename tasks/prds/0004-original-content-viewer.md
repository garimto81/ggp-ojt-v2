# PRD-0004: 원문 뷰어 + 퀴즈 출제 시스템

**상태**: ✅ 구현 완료 (v2.10.0)
**최종 업데이트**: 2025-12-07
**관련 이슈**: #81, #34

## 1. 개요

### 1.1 목적
PDF/URL 원문을 그대로 표시하고, WebLLM(브라우저 내 LLM)을 활용하여 퀴즈를 자동 생성하는 시스템

### 1.2 배경
- **현재 문제점**:
  - PDF/URL 텍스트 추출 후 AI가 "요약/재생성"하여 원문 손실
  - 이미지, 표, 레이아웃 등 시각적 요소 무시
  - CORS 프록시 의존으로 일부 사이트 접근 불가

- **해결책**:
  - 원문을 그대로 뷰어로 표시 (새 탭에서 열기)
  - WebLLM으로 텍스트 분석 후 퀴즈 생성 (무료, 오프라인 가능)
  - 원문 URL/PDF 링크를 별도 저장하여 언제든 접근 가능

### 1.3 범위
- URL 원문 뷰어 (새 탭에서 열기) ✅
- PDF 원문 뷰어 (새 탭에서 R2 URL 열기) ✅
- WebLLM 기반 퀴즈 생성 ✅
- 데이터 구조 변경 (source_type, source_url, source_file) ✅

---

## 2. 요구사항

### 2.1 기능 요구사항

#### FR-001: URL 자료 등록
- **설명**: Mentor가 URL을 입력하면 원문 URL 저장 + 퀴즈 자동 생성
- **입력**: URL 문자열
- **처리**:
  1. URL 유효성 검증
  2. Gemini URL Context Tool로 퀴즈 20개 생성
  3. source_type='url', source_url 저장
- **출력**: ojt_docs 레코드 생성

#### FR-002: PDF 자료 등록
- **설명**: Mentor가 PDF 업로드하면 R2 저장 + 퀴즈 자동 생성
- **입력**: PDF 파일
- **처리**:
  1. PDF를 Cloudflare R2에 업로드
  2. 공개 URL 획득
  3. Gemini URL Context Tool로 퀴즈 20개 생성
  4. source_type='pdf', source_file 저장
- **출력**: ojt_docs 레코드 생성

#### FR-003: URL 원문 뷰어
- **설명**: Mentee가 URL 자료의 원문을 볼 수 있음
- **구현 옵션**:
  - A) 새 탭에서 열기 (window.open)
  - B) x-frame-bypass 컴포넌트
- **우선순위**: 새 탭 열기 (100% 호환)

#### FR-004: PDF 원문 뷰어
- **설명**: Mentee가 PDF 자료의 원문을 볼 수 있음
- **구현**:
  - iframe + PDF.js 뷰어
  - `https://mozilla.github.io/pdf.js/web/viewer.html?file={R2_URL}`
- **대안**: react-pdf 라이브러리

#### FR-005: 퀴즈 응시
- **설명**: 기존 퀴즈 응시 기능 유지
- **변경 없음**: 현재 구현 그대로 사용

### 2.2 비기능 요구사항

#### NFR-001: 성능
- 퀴즈 생성 시간: 30초 이내
- PDF 업로드: 10MB 이내

#### NFR-002: 보안
- R2 업로드된 PDF는 공개 URL이지만 추측 불가능한 키 사용
- URL 입력 시 SSRF 방어 유지

#### NFR-003: 호환성
- 기존 자료(sections 기반)와 새 자료(source_url/source_file 기반) 공존
- 점진적 마이그레이션 지원

---

## 3. 기술 설계

### 3.1 데이터 구조 변경

```sql
-- ojt_docs 테이블 컬럼 추가
ALTER TABLE ojt_docs ADD COLUMN source_type TEXT; -- 'url', 'pdf', 'manual'
ALTER TABLE ojt_docs ADD COLUMN source_url TEXT;  -- URL인 경우 원본 URL
ALTER TABLE ojt_docs ADD COLUMN source_file TEXT; -- PDF인 경우 R2 URL
```

```javascript
// 새로운 ojt_docs 구조
{
  id: 'uuid',
  title: '자료 제목',
  team: '팀명',
  step: 1,

  // 새로운 필드
  source_type: 'url' | 'pdf' | 'manual',
  source_url: 'https://example.com/article',  // URL인 경우
  source_file: 'https://r2.dev/uploads/xxx.pdf',  // PDF인 경우

  // 기존 필드 (하위 호환)
  sections: [...],  // manual 타입 또는 기존 자료

  // 공통
  quiz: [...],
  estimated_minutes: 10,
  author_id: 'uuid',
  created_at: timestamp
}
```

### 3.2 WebLLM 기반 퀴즈 생성

> **변경사항**: Gemini URL Context Tool 대신 **WebLLM** 사용 (무료, 오프라인 가능)

```javascript
// src-vite/src/features/ai/services/contentGenerator.js
import { getWebLLMEngine } from './webllm';

export async function generateOJTContent(text, options = {}) {
  const { onProgress } = options;
  const engine = getWebLLMEngine();

  if (!engine) {
    // WebLLM 미로드 시 fallback 콘텐츠 반환
    return createFallbackContent(text, options);
  }

  const prompt = `다음 텍스트를 분석하여 OJT 교육 자료를 생성하세요.

텍스트:
${text}

요구사항:
- 섹션으로 구조화
- 퀴즈 20개 생성 (기억형 40%, 이해형 35%, 적용형 25%)
- JSON 형식 출력

출력 형식:
{
  "sections": [{ "title": "...", "content": "..." }],
  "quiz": [{ "question": "...", "options": [...], "correct": 0 }]
}`;

  const response = await engine.chat.completions.create({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.3,
    max_tokens: 4096,
  });

  return parseAIResponse(response.choices[0].message.content);
}
```

**WebLLM 모델 설정** (`constants.js`):
- Default: `Qwen2.5-3B-Instruct-q4f16_1-MLC` (한국어 우수, 2.4GB)
- Fallback: `gemma-2-2b-it-q4f16_1-MLC` (저사양용, 1.8GB)

### 3.3 UI 컴포넌트

#### MentorDashboard 변경

```jsx
// 자료 생성 폼
<div className="space-y-4">
  {/* 탭 선택 */}
  <div className="flex gap-2">
    <button onClick={() => setInputMode('url')}>URL 입력</button>
    <button onClick={() => setInputMode('pdf')}>PDF 업로드</button>
    <button onClick={() => setInputMode('manual')}>직접 작성</button>
  </div>

  {/* URL 입력 */}
  {inputMode === 'url' && (
    <input
      type="url"
      placeholder="https://example.com/article"
      value={urlInput}
      onChange={e => setUrlInput(e.target.value)}
    />
  )}

  {/* PDF 업로드 */}
  {inputMode === 'pdf' && (
    <input
      type="file"
      accept=".pdf"
      onChange={handlePdfSelect}
    />
  )}

  <button onClick={handleSubmit}>
    {inputMode === 'manual' ? '자료 생성' : '퀴즈 생성'}
  </button>
</div>
```

#### MenteeStudy 변경

```jsx
// 학습 화면
<div>
  {/* 원문 보기 버튼 */}
  {doc.source_type === 'url' && (
    <button onClick={() => window.open(doc.source_url, '_blank')}>
      원문 보기 (새 탭)
    </button>
  )}

  {doc.source_type === 'pdf' && (
    <button onClick={() => setShowPdfViewer(true)}>
      PDF 보기
    </button>
  )}

  {/* PDF 뷰어 모달 */}
  {showPdfViewer && (
    <Modal onClose={() => setShowPdfViewer(false)}>
      <iframe
        src={`https://mozilla.github.io/pdf.js/web/viewer.html?file=${encodeURIComponent(doc.source_file)}`}
        className="w-full h-[80vh]"
      />
    </Modal>
  )}

  {/* 퀴즈 시작 */}
  <button onClick={() => setShowQuiz(true)}>
    퀴즈 시작
  </button>
</div>
```

### 3.4 UI 목업 디자인

#### 3.4.1 Mentee 학습 화면 - 원문 보기 버튼

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 목록으로                                    🚨 신고   Step 1 │
│                                                                 │
│  React 기초 강좌                                                │
│  예상 학습 시간: 15분                                           │
│                                                                 │
│  ┌─────────────────────────────────────────┐                   │
│  │ [🔗 원문 보기]  또는  [📄 PDF 원문 보기] │ ← source_type별   │
│  └─────────────────────────────────────────┘                   │
│                                                                 │
│  섹션 2 / 3                                                    │
│  ████████████████████░░░░░░░░░░ 67% 완료                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  2. React 컴포넌트                                              │
│                                                                 │
│  React 컴포넌트는 UI를 독립적이고 재사용                         │
│  가능한 조각으로 나눈 것입니다.                                  │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  [← 이전 섹션]                                   [다음 섹션 →] │
└─────────────────────────────────────────────────────────────────┘
```

**source_type별 동작**:

| source_type | 버튼 | 클릭 동작 |
|-------------|------|----------|
| `url` | 🔗 원문 보기 | 새 탭에서 원본 URL 열기 |
| `pdf` | 📄 PDF 원문 보기 | 새 탭에서 R2 URL 열기 |
| `text` | 표시 안함 | - |

#### 3.4.2 Admin 콘텐츠 미리보기 패널

```
┌─────────────────────────────────────────────────────────────────┐
│  ContentPreviewPanel                                             │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  📄 React 기초 강좌                              [게시됨]  │ │
│  │  작성자: 홍길동 | 팀: 개발팀 | 2024.12.07                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  📎 원본 소스                                              │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  [🔗 원문 보기]  또는  [📄 PDF 원문 보기]            │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  [📖 섹션 (3)]  [📝 퀴즈 (10)]  [⚠️ 신고 (2)]             │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │  섹션 내용...                                              │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  [📤 게시]  [🙈 숨기기]  [🗑️ 삭제]                         │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.4.3 Mentor 입력 모드 탭

```
┌─────────────────────────────────────────────────────────────────┐
│  콘텐츠 입력                                                     │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  [텍스트]  [URL ●]  [PDF]                 ← 입력 모드 탭   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  📎 URL 입력                                                 ││
│  │                                                             ││
│  │  원본 웹페이지 URL을 입력하세요.                             ││
│  │  AI가 콘텐츠를 분석하여 교육 자료와 퀴즈를 생성합니다.       ││
│  │                                                             ││
│  │  ┌────────────────────────────────────────────────────────┐ ││
│  │  │ https://example.com/react-tutorial                     │ ││
│  │  └────────────────────────────────────────────────────────┘ ││
│  │                                                             ││
│  │  문서 제목 (선택)                                            ││
│  │  ┌────────────────────────────────────────────────────────┐ ││
│  │  │ [자동 추출 또는 직접 입력...]                           │ ││
│  │  └────────────────────────────────────────────────────────┘ ││
│  │                                                             ││
│  │  [🔗 URL에서 교육 자료 생성]                                 ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

#### 3.4.4 Mentor PDF 업로드 모드

```
┌─────────────────────────────────────────────────────────────────┐
│  콘텐츠 입력                                                     │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  [텍스트]  [URL]  [PDF ●]                                   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  📄 PDF 업로드                                               ││
│  │                                                             ││
│  │  ┌────────────────────────────────────────────────────────┐ ││
│  │  │                                                        │ ││
│  │  │    ┌─────────────────────────────────────────┐         │ ││
│  │  │    │         📁 파일 선택 또는 드래그        │         │ ││
│  │  │    │                                         │         │ ││
│  │  │    │  지원 형식: PDF (최대 10MB)             │         │ ││
│  │  │    └─────────────────────────────────────────┘         │ ││
│  │  │                                                        │ ││
│  │  └────────────────────────────────────────────────────────┘ ││
│  │                                                             ││
│  │  === 업로드 후 ===                                          ││
│  │  ┌────────────────────────────────────────────────────────┐ ││
│  │  │  ✅ react-tutorial.pdf (2.3MB)             [X 제거]    │ ││
│  │  └────────────────────────────────────────────────────────┘ ││
│  │                                                             ││
│  │  [📄 PDF에서 교육 자료 생성]                                 ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

#### 3.4.5 생성 진행 상태

```
┌─────────────────────────────────────────────────────────────────┐
│  === 생성 진행 중 ===                                           │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                                                             ││
│  │  🔄 교육 자료 생성 중...                                     ││
│  │                                                             ││
│  │  ████████████████████░░░░░░░░░░ 67%                         ││
│  │                                                             ││
│  │  현재 단계: 퀴즈 생성 중 (3/4)                               ││
│  │                                                             ││
│  │  • ✅ URL 텍스트 추출 완료                                   ││
│  │  • ✅ 섹션 구조화 완료                                       ││
│  │  • 🔄 퀴즈 생성 중...                                        ││
│  │  • ⏳ 퀴즈 검증 대기                                         ││
│  │                                                             ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

#### 3.4.6 URL/PDF 원문 보기 플로우

```
사용자 클릭: [🔗 원문 보기] 또는 [📄 PDF 원문 보기]
    │
    ▼
window.open(url, '_blank')
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  [새 브라우저 탭]                                                │
│                                                                 │
│  URL: https://example.com/react-tutorial                        │
│       또는 https://r2.ojt-master.dev/pdfs/abc123.pdf            │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │   원본 웹페이지 또는 브라우저 내장 PDF 뷰어                │ │
│  │                                                           │ │
│  │   - URL: 원본 사이트 그대로 표시                          │ │
│  │   - PDF: Chrome/Edge 내장 PDF 뷰어로 표시                 │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**새 탭 열기 방식의 장점**:
- X-Frame-Options 차단 사이트도 정상 표시
- CORS 이슈 없음
- 원본 사이트의 모든 기능 사용 가능
- 브라우저 내장 PDF 뷰어 활용 (확대/축소, 다운로드, 인쇄)

---

### 3.5 PDF R2 업로드 확장

```javascript
// 기존 이미지 업로드 함수 확장
async function uploadPdfToR2(file) {
  // 파일 타입 검증
  if (file.type !== 'application/pdf') {
    throw new Error('PDF 파일만 업로드 가능합니다');
  }

  // 파일 크기 검증 (10MB)
  if (file.size > 10 * 1024 * 1024) {
    throw new Error('파일 크기는 10MB를 초과할 수 없습니다');
  }

  // R2 Worker에 업로드 (기존 로직 재사용)
  const key = `pdfs/${Date.now()}-${Math.random().toString(36).substr(2, 8)}.pdf`;

  // ... 업로드 로직

  return publicUrl;
}
```

---

## 4. 마이그레이션 전략

### 4.1 하위 호환성
- 기존 자료(sections 기반)는 그대로 표시
- source_type이 없으면 'manual'로 간주
- 점진적 전환 (강제 마이그레이션 없음)

### 4.2 DB 마이그레이션

```sql
-- 1. 컬럼 추가 (nullable)
ALTER TABLE ojt_docs ADD COLUMN source_type TEXT DEFAULT 'manual';
ALTER TABLE ojt_docs ADD COLUMN source_url TEXT;
ALTER TABLE ojt_docs ADD COLUMN source_file TEXT;

-- 2. 기존 데이터 업데이트
UPDATE ojt_docs SET source_type = 'manual' WHERE source_type IS NULL;
```

---

## 5. R2 Worker 수정

### 5.1 PDF 업로드 지원 추가

```javascript
// ojt-r2-upload/src/index.js 수정
const ALLOWED_TYPES = [
  'image/jpeg', 'image/png', 'image/gif', 'image/webp',
  'application/pdf'  // PDF 추가
];

const MAX_FILE_SIZE = {
  image: 10 * 1024 * 1024,  // 10MB
  pdf: 10 * 1024 * 1024     // 10MB
};
```

---

## 6. 테스트 계획

### 6.1 단위 테스트
- [x] URL 유효성 검증 (`validateUrlForSSRF`)
- [x] PDF 업로드 함수 (`uploadImageToR2` 확장)
- [x] WebLLM 퀴즈 생성 (`generateOJTContent`)
- [x] Fallback 콘텐츠 생성 (`createFallbackContent`)

### 6.2 E2E 테스트
- [x] URL 입력 → 퀴즈 생성 → 원문 보기 → 퀴즈 응시
- [ ] PDF 업로드 → 퀴즈 생성 → PDF 보기 → 퀴즈 응시 (MentorDashboard PDF 업로드 스텁)
- [x] 기존 자료 호환성 확인

### 6.3 엣지 케이스
- [x] X-Frame-Options 차단 URL → 새 탭에서 열기로 해결
- [x] 큰 PDF 파일 (10MB 근처) → R2 Worker 제한
- [x] 한글 URL 인코딩 → encodeURIComponent
- [x] WebLLM 미로드 시 → fallbackContent 반환

---

## 7. 구현 현황

| 단계 | 작업 | 상태 | 커밋 |
|------|------|------|------|
| Phase 1 | DB 스키마 변경 (source_type, source_url, source_file) | ✅ 완료 | #34 |
| Phase 2 | WebLLM 퀴즈 생성 연동 | ✅ 완료 | #45 |
| Phase 3 | Mentor UI 변경 (MentorDashboard) | ✅ 완료 | #79 |
| Phase 4 | Mentee 원문 뷰어 (MenteeStudy) | ✅ 완료 | #81 |
| Phase 5 | Admin 콘텐츠 미리보기 (ContentPreviewPanel) | ✅ 완료 | #81 |
| Phase 6 | PDF 업로드 UI (MentorDashboard) | 🟡 스텁 | - |

### 7.1 Phase 6 구현 계획 (PDF 업로드 UI)

**목표**: MentorDashboard에 PDF 업로드 탭 추가

**구현 항목**:
1. 입력 모드 탭 UI (텍스트 / URL / PDF)
2. PDF 파일 선택 컴포넌트 (드래그 앤 드롭 지원)
3. R2 업로드 함수 호출 (`uploadImageToR2` 확장)
4. PDF 텍스트 추출 (`pdfjs-dist`)
5. WebLLM 퀴즈 생성 연동
6. 생성 진행 상태 표시

**UI 참조**: `docs/design/UI_MOCKUP_DESIGN.md` 섹션 6.7

**예상 작업량**: Medium (3-5시간)

---

## 8. 관련 파일

### 핵심 컴포넌트
| 파일 | 용도 |
|------|------|
| `src-vite/src/features/docs/components/MentorDashboard.jsx` | Mentor 자료 등록 |
| `src-vite/src/features/learning/components/MenteeStudy.jsx` | Mentee 학습 + 원문 보기 |
| `src-vite/src/features/admin/components/content/ContentPreviewPanel.jsx` | Admin 콘텐츠 미리보기 |
| `src-vite/src/features/docs/components/PdfViewer.jsx` | PDF 뷰어 (미사용) |
| `src-vite/src/features/docs/components/UrlPreviewPanel.jsx` | URL 미리보기 (미사용) |

### AI 서비스
| 파일 | 용도 |
|------|------|
| `src-vite/src/features/ai/services/webllm.js` | WebLLM 엔진 관리 |
| `src-vite/src/features/ai/services/contentGenerator.js` | OJT 콘텐츠 생성 |
| `src-vite/src/features/ai/services/quizValidator.js` | 퀴즈 검증/재생성 |
| `src-vite/src/features/ai/services/fallbackContent.js` | Fallback 콘텐츠 |

### 유틸리티
| 파일 | 용도 |
|------|------|
| `src-vite/src/utils/security/validateUrl.js` | SSRF 방어 |
| `src-vite/src/features/docs/services/urlExtractor.js` | URL 텍스트 추출 |
| `src-vite/src/utils/cors-proxy.js` | CORS 프록시 체인 |

---

## 9. 참고 자료

- [WebLLM 공식 문서](https://mlc.ai/web-llm/)
- [PDF.js 뷰어](https://mozilla.github.io/pdf.js/)
- [pdfjs-dist npm](https://www.npmjs.com/package/pdfjs-dist)
- [Cloudflare R2 Workers](https://developers.cloudflare.com/r2/)

---

## 10. 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|----------|
| 2025-12-02 | 1.0 | 초안 작성 (Gemini 기반) |
| 2025-12-07 | 2.0 | WebLLM으로 전환, 구현 완료 반영 |
| 2025-12-07 | 2.1 | Phase 6 구현 계획 추가, UI 목업 문서 참조 연결 |
| 2025-12-07 | 2.2 | 섹션 3.4 UI 목업 디자인 추가 (Mentee/Admin/Mentor 화면) |

---

## 11. 승인

| 역할 | 이름 | 날짜 | 서명 |
|------|------|------|------|
| 작성자 | Claude | 2025-12-02 | ✅ |
| 업데이트 | Claude | 2025-12-07 | ✅ |
| 검토자 | | | |
| 승인자 | | | |
