# Admin Page 전체 기획서

**버전**: 1.0.0
**작성일**: 2025-12-07
**상태**: 설계 완료

---

## 1. 개요

### 1.1 목적
OJT Master 관리자 대시보드의 전체 재설계. 사용자 관리, 콘텐츠 관리, 통계 분석 기능을 현대적인 UX 패턴으로 개선.

### 1.2 현재 상태 분석

| 탭 | 현재 기능 | 문제점 |
|----|-----------|--------|
| 사용자 관리 | 테이블 + 필터 + 페이지네이션 | 대량 작업 불편, 상세 정보 부족 |
| 콘텐츠 관리 | 테이블 + 삭제만 | **미리보기 없음**, 상태 관리 없음, 신고 시스템 없음 |
| 통계 | 차트 + 테이블 | 기능 양호, 내보내기 없음 |

### 1.3 목표

| 지표 | 현재 | 목표 |
|------|------|------|
| 콘텐츠 확인 시간 | ~30초 | ~3초 |
| 부적절 콘텐츠 처리 | 수동 요청 | 자동 신고 시스템 |
| WCAG 2.1 AA 준수 | 70% | 95% |
| 관리 작업 효율성 | 기준 없음 | 50% 향상 |

---

## 2. 전체 구조

### 2.1 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Header (고정)                                                              │
│  ┌───────────┬───────────────────────────────────────────────────────────┐ │
│  │ OJT Logo  │  [AI 상태]  [알림 🔔]  [Admin ▼]  [모드 ▼]                │ │
│  └───────────┴───────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📊 Summary Cards (4개)                                             │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐           │   │
│  │  │ 총 사용자 │ │ 총 문서   │ │ 학습 기록 │ │ 통과율    │           │   │
│  │  │    125    │ │    48     │ │   1,234   │ │   78%     │           │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Tab Navigation                                                      │   │
│  │  [👥 사용자 관리] [📄 콘텐츠 관리] [📊 통계] [⚙️ 설정]              │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                       │   │
│  │  Tab Content Area                                                    │   │
│  │                                                                       │   │
│  │                                                                       │   │
│  │                                                                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 탭 구성

| 탭 | 아이콘 | 주요 기능 |
|----|--------|----------|
| 사용자 관리 | 👥 | 역할 변경, 부서 관리, 계정 상태, 대량 작업 |
| 콘텐츠 관리 | 📄 | **Split View 미리보기**, 상태 관리, 신고 처리 |
| 통계 | 📊 | 차트, 진도 현황, 취약 파트, 내보내기 |
| 설정 | ⚙️ | 시스템 설정, 알림 설정, 로그 뷰어 (신규) |

---

## 3. 탭별 상세 설계

### 3.1 사용자 관리 탭

#### 3.1.1 UI 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  사용자 관리                                                                │
├─────────────────────────────────────────────────────────────────────────────┤
│  Toolbar                                                                    │
│  ┌────────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ 🔍 이름 검색... │ │ 모든 역할▼│ │ 모든 부서▼│ │ 20개씩 ▼ │ │ 📥 내보내기 │ │
│  └────────────────┘ └──────────┘ └──────────┘ └──────────┘ └─────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ ☑️ 전체 선택 (3명 선택됨)   [역할 일괄 변경 ▼] [부서 일괄 변경 ▼]    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌──┬────────────────┬─────────┬─────────┬───────────┬──────────────────┐  │
│  │☐ │ 이름 ↓         │ 역할    │ 부서    │ 가입일    │ 액션             │  │
│  ├──┼────────────────┼─────────┼─────────┼───────────┼──────────────────┤  │
│  │☑️│ 김철수         │ Mentee▼ │ 개발팀▼ │ 2025-01-15│ [활성화][삭제]   │  │
│  │☐ │ 이영희         │ Mentor▼ │ 기획팀▼ │ 2025-01-10│ [정지][삭제]     │  │
│  │☑️│ 박지민 (정지됨)│ Mentee▼ │ 운영팀▼ │ 2025-01-05│ [활성화][삭제]   │  │
│  └──┴────────────────┴─────────┴─────────┴───────────┴──────────────────┘  │
│                                                                             │
│  [이전] [1] [2] [3] ... [10] [다음]                   125명 중 20명 표시   │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 신규 기능

| 기능 | 설명 |
|------|------|
| **대량 선택** | 체크박스로 여러 사용자 선택 후 일괄 역할/부서 변경 |
| **내보내기** | CSV/Excel 형식으로 사용자 목록 다운로드 |
| **사용자 상세** | 행 클릭 시 사이드 패널에 상세 정보 표시 |
| **활동 로그** | 사용자별 최근 활동 이력 조회 |

#### 3.1.3 사용자 상세 사이드 패널

```
┌─────────────────────────────────────┐
│  👤 김철수                          │
│  ─────────────────────────────────  │
│  📧 kim@example.com                 │
│  🏢 개발팀 | Mentee                 │
│  📅 가입: 2025-01-15                │
│  ─────────────────────────────────  │
│  📊 학습 현황                       │
│  ┌─────────────────────────────┐   │
│  │ 진도: 12/48 (25%)           │   │
│  │ ████████░░░░░░░░░░░░░░░░░░░ │   │
│  └─────────────────────────────┘   │
│  평균 점수: 82점                    │
│  최근 학습: 2025-12-05             │
│  ─────────────────────────────────  │
│  🕐 최근 활동                       │
│  • 12-05 React Hooks 퀴즈 통과     │
│  • 12-03 TypeScript 기초 학습 시작 │
│  • 12-01 로그인                    │
│  ─────────────────────────────────  │
│  [역할 변경] [부서 변경] [정지]    │
└─────────────────────────────────────┘
```

---

### 3.2 콘텐츠 관리 탭 (핵심 개선)

#### 3.2.1 Split View 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  콘텐츠 관리                                                                │
├─────────────────────────────────────────────────────────────────────────────┤
│  Status Tabs                                                                │
│  [전체 48] [검토대기 🔴3] [신고됨 🟡2] [숨김 ⚫1]                           │
├─────────────────────────────────────────────────────────────────────────────┤
│  Toolbar                                                                    │
│  ┌────────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐                 │
│  │ 🔍 제목 검색... │ │ 모든 팀 ▼│ │모든 작성자▼│ │ 20개씩 ▼ │                 │
│  └────────────────┘ └──────────┘ └──────────┘ └──────────┘                 │
├───────────────────────────────────┬─────────────────────────────────────────┤
│  콘텐츠 목록 (40%)                │  미리보기 (60%)                         │
│  ─────────────────────────────────│─────────────────────────────────────────│
│  ┌─────────────────────────────┐  │  ┌─────────────────────────────────────┐│
│  │ 📄 React Hooks 입문         │  │  │ 📄 React Hooks 입문                 ││
│  │    김멘토 | 개발팀 | 3일전   │◀─│  │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ││
│  │    [검토대기] ⚠️ 신고 2건   │  │  │ 작성자: 김멘토 | 팀: 개발팀        ││
│  │    ─────────────────────────│  │  │ 생성일: 2025-12-04 | 수정일: -     ││
│  │    [👁️ 미리보기] [🗑️ 삭제] │  │  │ 상태: 검토대기 | 신고: 2건         ││
│  └─────────────────────────────┘  │  │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ││
│  ┌─────────────────────────────┐  │  │                                     ││
│  │ 📄 TypeScript 가이드        │  │  │ 📖 섹션 미리보기                    ││
│  │    박멘토 | 기획팀 | 1주전   │  │  │ ─────────────────────────────────── ││
│  │    [게시됨] ✅               │  │  │ 1. useState 기본 개념               ││
│  │    ─────────────────────────│  │  │    useState는 React의 기본적인...   ││
│  │    [👁️ 미리보기] [🗑️ 삭제] │  │  │                                     ││
│  └─────────────────────────────┘  │  │ 2. useEffect 활용법                 ││
│  ┌─────────────────────────────┐  │  │    컴포넌트의 생명주기와 연동...    ││
│  │ 📄 Git 브랜치 전략          │  │  │                                     ││
│  │    최멘토 | 운영팀 | 2주전   │  │  │ 3. 커스텀 훅 만들기                 ││
│  │    [게시됨] ✅               │  │  │    재사용 가능한 로직을 훅으로...   ││
│  │    ─────────────────────────│  │  │ ─────────────────────────────────── ││
│  │    [👁️ 미리보기] [🗑️ 삭제] │  │  │                                     ││
│  └─────────────────────────────┘  │  │ 📝 퀴즈 미리보기 (10문항)           ││
│                                   │  │ ─────────────────────────────────── ││
│                                   │  │ Q1. useState의 반환값은?            ││
│                                   │  │     A) 배열 ✓                       ││
│                                   │  │ Q2. useEffect 의존성 배열이         ││
│                                   │  │     빈 배열일 때 실행 시점은?       ││
│                                   │  │     A) 마운트 시 1회 ✓              ││
│                                   │  │                                     ││
│                                   │  │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ││
│                                   │  │ ⚠️ 신고 내역 (2건)                  ││
│                                   │  │ ─────────────────────────────────── ││
│                                   │  │ • 내용 부정확 (멘티A, 12-05)        ││
│                                   │  │ • 오래된 정보 (멘티B, 12-04)        ││
│                                   │  │ ─────────────────────────────────── ││
│                                   │  │                                     ││
│                                   │  │ [📤 게시] [👁️ 숨기기] [🗑️ 삭제]   ││
│                                   │  └─────────────────────────────────────┘│
│  ─────────────────────────────────│                                         │
│  [이전] [1] [2] [다음]  48개 중 20개│                                         │
└───────────────────────────────────┴─────────────────────────────────────────┘
```

#### 3.2.2 콘텐츠 상태 시스템

```
                                    ┌───────────┐
                                    │   draft   │ (멘토가 작성 중)
                                    │  🔵 임시  │
                                    └─────┬─────┘
                                          │ 저장 완료
                                          ▼
┌───────────┐                       ┌───────────┐
│  hidden   │◀─────── 반려 ─────────│  review   │ (관리자 검토 대기)
│  ⚫ 숨김  │                       │ 🟠 검토대기│
└───────────┘                       └─────┬─────┘
      ▲                                   │ 승인
      │                                   ▼
      │                             ┌───────────┐
      └──── 신고 3건 이상 ──────────│ published │ (공개됨)
                                    │  🟢 게시됨 │
                                    └───────────┘
```

| 상태 | 코드 | 색상 | 설명 | 멘티 접근 |
|------|------|------|------|----------|
| 임시저장 | `draft` | 🔵 Blue | 작성 중 (멘토만 조회) | ❌ |
| 검토대기 | `review` | 🟠 Orange | 관리자 승인 대기 | ❌ |
| 게시됨 | `published` | 🟢 Green | 정상 공개 | ✅ |
| 숨김 | `hidden` | ⚫ Gray | 비공개 처리 | ❌ |

#### 3.2.3 신고 시스템

**신고 사유 옵션**
- `inappropriate` - 부적절한 내용
- `outdated` - 오래된 정보
- `duplicate` - 중복 콘텐츠
- `spam` - 스팸/광고
- `other` - 기타

**자동 처리 규칙**
- 신고 3건 이상: 자동으로 `hidden` 상태 전환
- 관리자가 신고 검토 후 `resolved`(해결) 또는 `dismissed`(기각) 처리

#### 3.2.4 Quick Actions

| 액션 | 아이콘 | 동작 | 확인 방식 |
|------|--------|------|----------|
| 미리보기 | 👁️ | 오른쪽 패널에 상세 표시 | 즉시 |
| 게시 | 📤 | `published` 상태로 변경 | 단순 확인 |
| 숨기기 | 👁️ | `hidden` 상태로 변경 | 단순 확인 |
| 삭제 | 🗑️ | 영구 삭제 | 제목 입력 확인 |

---

### 3.3 통계 탭

#### 3.3.1 현재 구조 (유지)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  통계                                                [📥 리포트 내보내기]   │
├─────────────────────────────────────────────────────────────────────────────┤
│  Overview Cards                                                             │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐                   │
│  │ 총 멘티   │ │ 활동 멘티 │ │ 전체 완료 │ │ 평균 진도 │                   │
│  │   45명    │ │   38명    │ │   12명    │ │   65%     │                   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘                   │
│                                                                             │
│  ┌─────────────────────────────────┐ ┌─────────────────────────────────┐   │
│  │  최근 7일 학습 활동             │ │  통과율                         │   │
│  │  ▓▓▓                           │ │         ████████                │   │
│  │  ▓▓▓  ▓▓▓                      │ │       ██████████                │   │
│  │  ▓▓▓  ▓▓▓  ▓▓▓     ▓▓▓        │ │  78%                            │   │
│  │  Mon  Tue  Wed  Thu  Fri  Sat  │ │                                 │   │
│  └─────────────────────────────────┘ └─────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────┐ ┌─────────────────────────────────┐   │
│  │  멘토 기여도                    │ │  진도 분포                      │   │
│  │  김멘토 ████████████ 15        │ │  0-25%   ██████   12명          │   │
│  │  박멘토 ██████████ 12          │ │  26-50%  ████████ 18명          │   │
│  │  이멘토 ██████ 8               │ │  51-75%  ████ 8명               │   │
│  └─────────────────────────────────┘ │  76-100% ██████ 7명             │   │
│                                       └─────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  팀별 통계                                                           │   │
│  │  개발팀  ██████████████████████ 문서: 20, 완료: 156, 진도: 72%      │   │
│  │  기획팀  ████████████████ 문서: 15, 완료: 89, 진도: 65%             │   │
│  │  운영팀  ████████████ 문서: 10, 완료: 45, 진도: 58%                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  취약 파트 분석 (실패율 높은 문서)                                   │   │
│  │  ┌──────────────────┬────────┬──────┬─────────┬──────────┐          │   │
│  │  │ 문서             │ 팀     │ 시도 │ 실패율  │ 평균점수 │          │   │
│  │  ├──────────────────┼────────┼──────┼─────────┼──────────┤          │   │
│  │  │ 고급 TypeScript  │ 개발팀 │ 45   │ 62% 🔴  │ 58점     │          │   │
│  │  │ 마이크로서비스   │ 개발팀 │ 32   │ 48% 🟡  │ 65점     │          │   │
│  │  │ 디자인 시스템    │ 기획팀 │ 28   │ 35% 🟡  │ 72점     │          │   │
│  │  └──────────────────┴────────┴──────┴─────────┴──────────┘          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  멘티별 진도 현황                                                    │   │
│  │  ┌──────────┬────────┬─────────┬──────────────────────┬──────────┐  │   │
│  │  │ 이름     │ 부서   │ 완료/전체│ 진도율              │ 평균점수 │  │   │
│  │  ├──────────┼────────┼─────────┼──────────────────────┼──────────┤  │   │
│  │  │ 김철수   │ 개발팀 │ 48/48   │ ████████████████ 100%│ 92점     │  │   │
│  │  │ 이영희   │ 기획팀 │ 35/48   │ ████████████░░░ 73%  │ 85점     │  │   │
│  │  │ 박지민   │ 운영팀 │ 12/48   │ ████░░░░░░░░░░ 25%   │ 78점     │  │   │
│  │  └──────────┴────────┴─────────┴──────────────────────┴──────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 3.3.2 신규 기능: 리포트 내보내기

| 형식 | 내용 |
|------|------|
| **PDF** | 전체 대시보드 스냅샷 (차트 포함) |
| **Excel** | 멘티별 진도, 취약 파트, 팀별 통계 시트 |
| **CSV** | 원시 데이터 (학습 기록, 퀴즈 결과) |

---

### 3.4 설정 탭 (신규)

#### 3.4.1 UI 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  설정                                                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📋 시스템 설정                                                      │   │
│  │  ─────────────────────────────────────────────────────────────────── │   │
│  │  기본 부서 목록                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────────┐    │   │
│  │  │ 개발팀, 디자인팀, 기획팀, 마케팅팀, 운영팀, 인사팀 [편집]   │    │   │
│  │  └─────────────────────────────────────────────────────────────┘    │   │
│  │                                                                       │   │
│  │  신규 가입자 기본 역할    [Mentee ▼]                                 │   │
│  │  퀴즈 통과 기준 점수      [70 ▼] 점                                  │   │
│  │  자동 숨김 신고 건수      [3 ▼] 건                                   │   │
│  │                                                                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  🔔 알림 설정                                                        │   │
│  │  ─────────────────────────────────────────────────────────────────── │   │
│  │  ☑️ 신규 가입자 알림                                                 │   │
│  │  ☑️ 콘텐츠 신고 알림                                                 │   │
│  │  ☐ 일일 통계 요약 메일                                               │   │
│  │  ☐ 주간 리포트 메일                                                  │   │
│  │                                                                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📜 시스템 로그                                                      │   │
│  │  ─────────────────────────────────────────────────────────────────── │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │ 2025-12-07 10:30:15  [INFO]  사용자 김철수 역할 변경 (Admin)  │  │   │
│  │  │ 2025-12-07 10:28:42  [WARN]  문서 "React Hooks" 신고 2건 누적 │  │   │
│  │  │ 2025-12-07 10:25:00  [INFO]  신규 가입: 박지민 (Mentee)       │  │   │
│  │  │ 2025-12-07 09:45:12  [INFO]  문서 "Git 전략" 상태 변경: hidden│  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                               [더 보기] [내보내기]  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 데이터베이스 스키마

### 4.1 기존 테이블 수정

```sql
-- ======================================
-- ojt_docs 테이블 확장
-- ======================================
ALTER TABLE ojt_docs
  ADD COLUMN IF NOT EXISTS status TEXT DEFAULT 'published'
    CHECK (status IN ('draft', 'review', 'published', 'hidden')),
  ADD COLUMN IF NOT EXISTS report_count INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS last_reviewed_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS reviewed_by UUID REFERENCES users(id);

-- 기존 데이터: 모두 published로 설정
UPDATE ojt_docs SET status = 'published' WHERE status IS NULL;

-- 인덱스
CREATE INDEX IF NOT EXISTS idx_ojt_docs_status ON ojt_docs(status);
CREATE INDEX IF NOT EXISTS idx_ojt_docs_report_count ON ojt_docs(report_count) WHERE report_count > 0;


-- ======================================
-- users 테이블 확장 (활동 추적)
-- ======================================
ALTER TABLE users
  ADD COLUMN IF NOT EXISTS last_active_at TIMESTAMPTZ;

CREATE INDEX IF NOT EXISTS idx_users_last_active ON users(last_active_at);
```

### 4.2 신규 테이블

```sql
-- ======================================
-- content_reports: 콘텐츠 신고 테이블
-- ======================================
CREATE TABLE IF NOT EXISTS content_reports (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  doc_id UUID NOT NULL REFERENCES ojt_docs(id) ON DELETE CASCADE,
  reporter_id UUID NOT NULL REFERENCES users(id),
  reason TEXT NOT NULL CHECK (reason IN ('inappropriate', 'outdated', 'duplicate', 'spam', 'other')),
  description TEXT,
  status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'resolved', 'dismissed')),
  created_at TIMESTAMPTZ DEFAULT now(),
  resolved_at TIMESTAMPTZ,
  resolved_by UUID REFERENCES users(id)
);

-- RLS 정책
ALTER TABLE content_reports ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can create reports" ON content_reports
  FOR INSERT TO authenticated
  WITH CHECK (reporter_id = auth.uid());

CREATE POLICY "Admins can view all reports" ON content_reports
  FOR SELECT TO authenticated
  USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

CREATE POLICY "Admins can update reports" ON content_reports
  FOR UPDATE TO authenticated
  USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

-- 인덱스
CREATE INDEX idx_content_reports_doc_id ON content_reports(doc_id);
CREATE INDEX idx_content_reports_status ON content_reports(status) WHERE status = 'pending';


-- ======================================
-- admin_settings: 시스템 설정 테이블
-- ======================================
CREATE TABLE IF NOT EXISTS admin_settings (
  key TEXT PRIMARY KEY,
  value JSONB NOT NULL,
  updated_at TIMESTAMPTZ DEFAULT now(),
  updated_by UUID REFERENCES users(id)
);

-- 기본 설정값 삽입
INSERT INTO admin_settings (key, value) VALUES
  ('default_departments', '["개발팀", "디자인팀", "기획팀", "마케팅팀", "운영팀", "인사팀"]'),
  ('default_role', '"mentee"'),
  ('quiz_pass_score', '70'),
  ('auto_hide_report_count', '3')
ON CONFLICT (key) DO NOTHING;


-- ======================================
-- admin_logs: 관리자 활동 로그
-- ======================================
CREATE TABLE IF NOT EXISTS admin_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  admin_id UUID NOT NULL REFERENCES users(id),
  action TEXT NOT NULL,
  target_type TEXT NOT NULL CHECK (target_type IN ('user', 'doc', 'report', 'setting')),
  target_id UUID,
  details JSONB,
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX idx_admin_logs_created ON admin_logs(created_at DESC);
CREATE INDEX idx_admin_logs_admin ON admin_logs(admin_id);


-- ======================================
-- 트리거: 신고 수 자동 업데이트
-- ======================================
CREATE OR REPLACE FUNCTION update_doc_report_count()
RETURNS TRIGGER AS $$
BEGIN
  IF TG_OP = 'INSERT' THEN
    UPDATE ojt_docs SET report_count = report_count + 1 WHERE id = NEW.doc_id;
    -- 3건 이상 신고 시 자동 숨김 (설정값 참조)
    UPDATE ojt_docs
    SET status = 'hidden'
    WHERE id = NEW.doc_id
      AND report_count >= (SELECT (value::integer) FROM admin_settings WHERE key = 'auto_hide_report_count')
      AND status = 'published';
  ELSIF TG_OP = 'DELETE' THEN
    UPDATE ojt_docs SET report_count = GREATEST(report_count - 1, 0) WHERE id = OLD.doc_id;
  END IF;
  RETURN NULL;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS tr_update_report_count ON content_reports;
CREATE TRIGGER tr_update_report_count
AFTER INSERT OR DELETE ON content_reports
FOR EACH ROW EXECUTE FUNCTION update_doc_report_count();
```

### 4.3 ERD

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│     users       │       │    ojt_docs     │       │ content_reports │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id (PK)         │◄──┐   │ id (PK)         │◄──────│ doc_id (FK)     │
│ name            │   │   │ title           │       │ reporter_id(FK) │───┐
│ role            │   │   │ team            │       │ reason          │   │
│ department      │   └───│ author_id (FK)  │       │ description     │   │
│ is_active       │       │ status ★        │       │ status          │   │
│ last_active_at★ │       │ report_count ★  │       │ resolved_by(FK) │───┤
│ created_at      │       │ reviewed_by (FK)│───┐   │ created_at      │   │
└─────────────────┘       │ created_at      │   │   └─────────────────┘   │
        ▲                 └─────────────────┘   │                         │
        │                                       │                         │
        └───────────────────────────────────────┴─────────────────────────┘

┌─────────────────┐       ┌─────────────────┐
│ admin_settings  │       │   admin_logs    │
├─────────────────┤       ├─────────────────┤
│ key (PK)        │       │ id (PK)         │
│ value (JSONB)   │       │ admin_id (FK)   │
│ updated_at      │       │ action          │
│ updated_by (FK) │       │ target_type     │
└─────────────────┘       │ target_id       │
                          │ details (JSONB) │
                          │ created_at      │
                          └─────────────────┘

★ = 신규 컬럼
```

---

## 5. 컴포넌트 구조

### 5.1 파일 트리

```
src-vite/src/features/admin/
├── components/
│   ├── AdminDashboard.jsx              # 메인 컨테이너 (탭 관리)
│   ├── SummaryCards.jsx                # 상단 통계 카드 (분리)
│   │
│   ├── users/                          # 사용자 관리 탭
│   │   ├── UsersManagementTab.jsx      # 탭 컨테이너
│   │   ├── UsersTable.jsx              # 테이블 컴포넌트
│   │   ├── UserDetailPanel.jsx         # 사이드 패널 (신규)
│   │   └── BulkActionsBar.jsx          # 대량 작업 바 (신규)
│   │
│   ├── content/                        # 콘텐츠 관리 탭
│   │   ├── ContentManagementTab.jsx    # Split View 컨테이너
│   │   ├── ContentListPanel.jsx        # 왼쪽 목록 패널
│   │   ├── ContentPreviewPanel.jsx     # 오른쪽 미리보기 패널
│   │   ├── ContentStatusBadge.jsx      # 상태 배지
│   │   ├── ContentQuickActions.jsx     # 인라인 액션 버튼
│   │   └── ContentReportsModal.jsx     # 신고 내역 모달
│   │
│   ├── stats/                          # 통계 탭
│   │   ├── StatsTab.jsx                # 탭 컨테이너
│   │   ├── AnalyticsCharts.jsx         # 차트 컴포넌트들 (기존)
│   │   └── ExportReportButton.jsx      # 리포트 내보내기 (신규)
│   │
│   └── settings/                       # 설정 탭 (신규)
│       ├── SettingsTab.jsx             # 탭 컨테이너
│       ├── SystemSettings.jsx          # 시스템 설정
│       ├── NotificationSettings.jsx    # 알림 설정
│       └── AdminLogsViewer.jsx         # 로그 뷰어
│
├── hooks/
│   ├── useAnalytics.js                 # 통계 로직 (기존)
│   ├── useUsers.js                     # 사용자 관리 로직 (기존)
│   ├── useContentManagement.js         # 콘텐츠 관리 로직 (신규)
│   ├── useContentReports.js            # 신고 관리 로직 (신규)
│   └── useAdminSettings.js             # 설정 관리 로직 (신규)
│
└── services/
    └── adminApi.js                     # Admin API 함수 (신규)
```

### 5.2 컴포넌트 의존성

```
AdminDashboard.jsx
├── SummaryCards.jsx
├── UsersManagementTab.jsx
│   ├── UsersTable.jsx
│   ├── UserDetailPanel.jsx
│   ├── BulkActionsBar.jsx
│   └── Pagination.jsx (공통)
├── ContentManagementTab.jsx
│   ├── Allotment (외부 라이브러리)
│   ├── ContentListPanel.jsx
│   │   ├── ContentStatusBadge.jsx
│   │   └── ContentQuickActions.jsx
│   ├── ContentPreviewPanel.jsx
│   └── ContentReportsModal.jsx
├── StatsTab.jsx
│   ├── AnalyticsCharts.jsx
│   │   ├── ActivityChart
│   │   ├── PassRateChart
│   │   ├── MentorContributionChart
│   │   ├── ProgressDistributionChart
│   │   └── TeamStatsChart
│   └── ExportReportButton.jsx
└── SettingsTab.jsx (신규)
    ├── SystemSettings.jsx
    ├── NotificationSettings.jsx
    └── AdminLogsViewer.jsx
```

---

## 6. API 설계

### 6.1 React Query Keys

```javascript
// features/admin/hooks/keys.js

// 사용자 관리
export const usersKeys = {
  all: ['admin', 'users'],
  lists: () => [...usersKeys.all, 'list'],
  list: (filters) => [...usersKeys.lists(), filters],
  detail: (id) => [...usersKeys.all, 'detail', id],
  activity: (id) => [...usersKeys.all, 'activity', id],
};

// 콘텐츠 관리
export const contentKeys = {
  all: ['admin', 'content'],
  lists: () => [...contentKeys.all, 'list'],
  list: (filters) => [...contentKeys.lists(), filters],
  detail: (id) => [...contentKeys.all, 'detail', id],
  reports: (docId) => [...contentKeys.all, 'reports', docId],
};

// 설정
export const settingsKeys = {
  all: ['admin', 'settings'],
  system: () => [...settingsKeys.all, 'system'],
  logs: (page) => [...settingsKeys.all, 'logs', page],
};
```

### 6.2 API 엔드포인트

| Method | Endpoint | 설명 | 권한 |
|--------|----------|------|------|
| **사용자** ||||
| GET | `/users` | 사용자 목록 (필터/페이지네이션) | Admin |
| PATCH | `/users/:id` | 사용자 정보 수정 (역할/부서) | Admin |
| PATCH | `/users/bulk` | 대량 사용자 수정 | Admin |
| DELETE | `/users/:id` | 사용자 삭제 | Admin |
| GET | `/users/:id/activity` | 사용자 활동 로그 | Admin |
| **콘텐츠** ||||
| GET | `/docs?status=...` | 상태별 문서 조회 | Admin |
| PATCH | `/docs/:id/status` | 문서 상태 변경 | Admin |
| GET | `/docs/:id/reports` | 문서 신고 목록 | Admin |
| POST | `/docs/:id/reports` | 신고 등록 | 인증된 사용자 |
| PATCH | `/reports/:id` | 신고 처리 | Admin |
| **설정** ||||
| GET | `/settings` | 시스템 설정 조회 | Admin |
| PATCH | `/settings/:key` | 설정값 변경 | Admin |
| GET | `/logs` | 관리자 활동 로그 | Admin |

### 6.3 Supabase 함수 예시

```javascript
// features/admin/services/adminApi.js

import { supabase } from '@utils/api';

// 콘텐츠 상태 변경
export async function updateDocStatus(docId, newStatus, adminId) {
  const { data, error } = await supabase
    .from('ojt_docs')
    .update({
      status: newStatus,
      last_reviewed_at: new Date().toISOString(),
      reviewed_by: adminId
    })
    .eq('id', docId)
    .select()
    .single();

  if (error) throw error;

  // 로그 기록
  await logAdminAction(adminId, 'status_change', 'doc', docId, { newStatus });

  return data;
}

// 신고 처리
export async function resolveReport(reportId, action, adminId) {
  const { data, error } = await supabase
    .from('content_reports')
    .update({
      status: action, // 'resolved' or 'dismissed'
      resolved_at: new Date().toISOString(),
      resolved_by: adminId
    })
    .eq('id', reportId)
    .select()
    .single();

  if (error) throw error;
  return data;
}

// 관리자 활동 로그 기록
export async function logAdminAction(adminId, action, targetType, targetId, details) {
  const { error } = await supabase
    .from('admin_logs')
    .insert({
      admin_id: adminId,
      action,
      target_type: targetType,
      target_id: targetId,
      details
    });

  if (error) console.error('Log error:', error);
}
```

---

## 7. 접근성 (A11y)

### 7.1 ARIA 적용

```jsx
// Tab Navigation
<div role="tablist" aria-label="관리자 대시보드 탭">
  <button role="tab" aria-selected={active} aria-controls="panel-users">
    사용자 관리
  </button>
</div>

// Tab Panel
<div role="tabpanel" id="panel-users" aria-labelledby="tab-users">
  ...
</div>

// Split View
<div role="region" aria-label="콘텐츠 목록">
  <ul role="listbox" aria-label="문서 목록">
    <li role="option" aria-selected={isSelected}>...</li>
  </ul>
</div>

<div role="region" aria-label="콘텐츠 미리보기" aria-live="polite">
  ...
</div>

// Status Badge
<span role="status" aria-label={`상태: ${statusLabel}`}>
  {statusLabel}
</span>

// Data Table
<table aria-label="사용자 목록">
  <thead>
    <tr>
      <th scope="col" aria-sort={sortDirection}>이름</th>
    </tr>
  </thead>
</table>
```

### 7.2 키보드 네비게이션

| 키 | 컨텍스트 | 동작 |
|----|---------|------|
| `Tab` | 전체 | 탭 간 이동, 목록 → 미리보기 → 액션 |
| `↑/↓` | 목록 | 항목 선택 |
| `Enter` | 목록 항목 | 미리보기 표시 |
| `Space` | 체크박스 | 선택/해제 |
| `Escape` | 모달/드롭다운 | 닫기 |
| `Ctrl+A` | 테이블 | 전체 선택 |

### 7.3 색상 대비

| 요소 | 전경색 | 배경색 | 비율 | WCAG |
|------|--------|--------|------|------|
| 본문 텍스트 | `#374151` | `#FFFFFF` | 10.9:1 | AAA |
| 보조 텍스트 | `#6B7280` | `#FFFFFF` | 4.6:1 | AA |
| 링크 | `#2563EB` | `#FFFFFF` | 4.8:1 | AA |
| 오류 | `#DC2626` | `#FFFFFF` | 4.5:1 | AA |

---

## 8. 보안

### 8.1 권한 검증

```javascript
// 모든 Admin API 호출 전 검증
function requireAdmin(user) {
  if (!user || user.role !== 'admin') {
    throw new Error('관리자 권한이 필요합니다.');
  }
}

// 컴포넌트 레벨 가드
function AdminDashboard() {
  const { user } = useAuth();

  if (user?.role !== 'admin') {
    return <Navigate to="/" replace />;
  }

  // ...
}
```

### 8.2 위험 작업 확인

| 작업 | 확인 방식 |
|------|----------|
| 역할 변경 | 단순 확인 다이얼로그 |
| 계정 정지 | 단순 확인 다이얼로그 |
| 사용자 삭제 | 2단계 확인 (이름 입력) |
| 문서 삭제 | 2단계 확인 (제목 입력) |
| 대량 삭제 | 2단계 확인 + 영향 범위 표시 |

### 8.3 활동 로깅

모든 관리자 작업은 `admin_logs` 테이블에 기록:
- 역할/부서 변경
- 계정 상태 변경
- 사용자/문서 삭제
- 설정 변경
- 신고 처리

---

## 9. 구현 계획

### Phase 1: 기반 구축 (2일)

- [ ] DB 마이그레이션 스크립트 작성 및 실행
- [ ] Allotment 라이브러리 설치
- [ ] 컴포넌트 폴더 구조 생성
- [ ] AdminDashboard에서 탭별 컴포넌트 분리

### Phase 2: 콘텐츠 관리 탭 (3일)

- [ ] ContentManagementTab 스켈레톤
- [ ] ContentListPanel (상태 필터, Quick Actions)
- [ ] ContentPreviewPanel (섹션, 퀴즈, 신고 내역)
- [ ] ContentStatusBadge, ContentQuickActions
- [ ] ContentReportsModal

### Phase 3: 사용자 관리 탭 개선 (2일)

- [ ] UsersManagementTab 분리
- [ ] UserDetailPanel (사이드 패널)
- [ ] BulkActionsBar (대량 선택/수정)
- [ ] 내보내기 기능

### Phase 4: 설정 탭 (1일)

- [ ] SettingsTab 컨테이너
- [ ] SystemSettings
- [ ] NotificationSettings
- [ ] AdminLogsViewer

### Phase 5: 통계 탭 개선 (1일)

- [ ] StatsTab 분리
- [ ] ExportReportButton (PDF/Excel/CSV)

### Phase 6: 테스트 및 문서화 (2일)

- [ ] E2E 테스트 작성 (Playwright)
- [ ] 접근성 테스트 (axe-core)
- [ ] 사용자 가이드 작성

---

## 10. 외부 의존성

### 10.1 신규 라이브러리

| 라이브러리 | 버전 | 용도 | 크기 |
|-----------|------|------|------|
| `allotment` | ^1.20.0 | Split View | ~15KB |
| `file-saver` | ^2.0.5 | 파일 다운로드 | ~3KB |
| `xlsx` | ^0.18.5 | Excel 내보내기 | ~200KB |
| `jspdf` | ^2.5.1 | PDF 내보내기 | ~300KB |

### 10.2 설치 명령어

```bash
cd src-vite
npm install allotment file-saver xlsx jspdf
```

---

## 11. 성공 지표

| 지표 | 현재 | 목표 | 측정 방법 |
|------|------|------|----------|
| 콘텐츠 확인 시간 | ~30초 | ~3초 | 사용자 테스트 |
| 부적절 콘텐츠 처리 | 수동 | 자동 | 신고 시스템 도입 |
| 관리 작업 효율성 | 기준 없음 | +50% | 작업당 클릭 수 측정 |
| WCAG 2.1 AA 준수 | 70% | 95% | axe-core 자동 검증 |
| Lighthouse 접근성 | 75점 | 95점 | Lighthouse CI |
| E2E 테스트 커버리지 | 30% | 80% | Playwright 리포트 |

---

## 12. 참고 자료

### 12.1 벤치마크 서비스

- **WordPress**: Post 목록 + Quick Edit + Status Workflow
- **Strapi**: Split View Content Manager
- **Notion**: Inline Preview + Database Views
- **Ghost CMS**: Post Status Workflow
- **Sanity Studio**: Real-time Preview

### 12.2 디자인 시스템

- **shadcn/ui**: https://ui.shadcn.com/
- **Radix UI**: https://www.radix-ui.com/
- **Tailwind CSS**: https://tailwindcss.com/

### 12.3 접근성

- **WCAG 2.1**: https://www.w3.org/WAI/WCAG21/quickref/
- **React Aria**: https://react-spectrum.adobe.com/react-aria/
- **axe-core**: https://github.com/dequelabs/axe-core

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0.0 | 2025-12-07 | 전체 Admin 페이지 기획서 작성 |
