# Architecture: OJT Master Block Agent System

**Version**: 1.1.0 | **Date**: 2025-12-09 | **Status**: Draft

> ⚠️ **v1.1.0 변경사항**: 코드 복잡도 분석 결과, 5개 도메인 에이전트를 **7개 세분화된 에이전트**로 재설계

> 로직 무결성과 AI 컨텍스트 최적화를 위한 블럭화 + 에이전트 시스템 아키텍처

---

## 목차

1. [개요](#1-개요)
2. [도메인 분석](#2-도메인-분석)
3. [블럭 정의](#3-블럭-정의)
4. [에이전트 시스템](#4-에이전트-시스템)
5. [Vertical Slicing 구조](#5-vertical-slicing-구조)
6. [Agent Rules](#6-agent-rules)
7. [구현 로드맵](#7-구현-로드맵)

---

## 1. 개요

### 1.1 목적

| 목표 | 설명 |
|------|------|
| **로직 무결성** | 블럭 간 명확한 경계로 부작용(Side Effect) 격리 |
| **AI 컨텍스트 최적화** | 블럭별 15-30개 파일로 AI 집중력 유지 |
| **관리 최적화** | 블럭별 전담 에이전트로 책임 분리 |
| **확장성** | 새 블럭/에이전트 추가 시 기존 시스템 영향 최소화 |

### 1.2 설계 원칙

```
┌─────────────────────────────────────────────────────────────┐
│                    DESIGN PRINCIPLES                         │
├─────────────────────────────────────────────────────────────┤
│  1. Single Responsibility  - 하나의 블럭은 하나의 관심사     │
│  2. Loose Coupling         - 블럭 간 인터페이스로만 통신     │
│  3. High Cohesion          - 블럭 내부는 강하게 결합        │
│  4. Fail Fast              - 에러는 즉시 감지, 빠르게 복구  │
│  5. Observable             - 모든 블럭 상태는 관찰 가능     │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 아키텍처 개요 (v1.1.0 - 7 Agents)

```
                              ┌─────────────────┐
                              │  Orchestrator   │
                              │     Agent       │
                              └────────┬────────┘
                                       │
    ┌──────────────┬──────────────┬────┴────┬──────────────┬──────────────┐
    │              │              │         │              │              │
    ▼              ▼              ▼         ▼              ▼              ▼
┌────────┐  ┌───────────┐  ┌───────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│  Auth  │  │ Content   │  │ Content   │  │Learning│  │Learning│  │  AI    │
│ Agent  │  │ Create    │  │ Manage    │  │ Study  │  │ Quiz   │  │ Agent  │
│        │  │ Agent     │  │ Agent     │  │ Agent  │  │ Agent  │  │        │
└───┬────┘  └─────┬─────┘  └─────┬─────┘  └───┬────┘  └───┬────┘  └───┬────┘
    │             │              │            │           │           │
┌───┴───┐   ┌─────┴─────┐  ┌─────┴─────┐ ┌───┴───┐  ┌────┴────┐ ┌───┴───┐
│login  │   │input      │  │document   │ │roadmap│  │quiz     │ │engine │
│session│   │generate   │  │quiz-mgmt  │ │section│  │record   │ │gemini │
│role   │   │preview    │  │           │ │       │  │         │ │webllm │
└───────┘   └───────────┘  └───────────┘ └───────┘  └─────────┘ └───────┘

                              ┌────────┐
                              │ Admin  │
                              │ Agent  │
                              └───┬────┘
                                  │
                             ┌────┴────┐
                             │users    │
                             │contents │
                             │stats    │
                             └─────────┘
```

### 1.4 v1.0.0 → v1.1.0 변경 이유

| 문제 파일 | LOC | 혼재된 관심사 | 해결책 |
|-----------|-----|--------------|--------|
| `MentorDashboard.jsx` | 661 | 입력/생성/저장/미리보기 | Content를 Create + Manage로 분리 |
| `MenteeStudy.jsx` | 475 | 학습/퀴즈/기록 | Learning을 Study + Quiz로 분리 |
| `api.js` | 628 | AI/문서/URL/이미지 | 도메인별 서비스로 분리 |

### 1.5 AI 컨텍스트 최적화

현재 OJT Master는 단일 폴더 구조로 **33개 파일**이 있습니다. 이는 AI 작업 시 다음 문제를 야기합니다:

| 문제 | 설명 |
|------|------|
| **Context Pollution** | 인증 수정 시 AI가 불필요한 학습 로직 파일도 로드 |
| **Attention Dilution** | 파일이 많을수록 AI 집중력 저하 |
| **Scale Threshold** | 50개+ 파일 시 AI 퍼포먼스 급락 |

**블럭화 효과:**

| Before (단일 구조) | After (블럭화) |
|-------------------|----------------|
| 전체 33개 파일 로드 | 해당 블럭만 8-12개 파일 |
| 무관한 코드로 인한 환각 | 관련 코드만 집중 분석 |
| AI 응답 시간 느림 | AI 응답 시간 빠름 |

---

## 2. 도메인 분석

### 2.1 현재 코드베이스 분석

```
src-vite/src/
├── components/          # 9개 컴포넌트 (혼재)
│   ├── AdminDashboard.jsx      → Admin 도메인
│   ├── AIEngineSelector.jsx    → AI 도메인
│   ├── Header.jsx              → Shared
│   ├── MenteeList.jsx          → Learning 도메인
│   ├── MenteeStudy.jsx         → Learning 도메인
│   ├── MentorDashboard.jsx     → Content 도메인
│   ├── PdfViewer.jsx           → Content 도메인
│   ├── RoleSelectionPage.jsx   → Auth 도메인
│   ├── SplitViewLayout.jsx     → Shared
│   └── UrlPreviewPanel.jsx     → Content 도메인
├── contexts/            # 4개 Context (혼재)
│   ├── AIContext.jsx           → AI 도메인
│   ├── AuthContext.jsx         → Auth 도메인
│   ├── DocsContext.jsx         → Content 도메인
│   └── ToastContext.jsx        → Shared
└── utils/               # 5개 유틸 (혼재)
    ├── api.js                  → 여러 도메인 혼재
    ├── db.js                   → Shared
    ├── webllm.js               → AI 도메인
    ├── cors-proxy.js           → Shared
    └── helpers.js              → Shared
```

### 2.2 도메인 식별

OJT Master는 **5개 도메인**으로 분류됩니다:

| 도메인 | 책임 | 현재 파일 |
|--------|------|----------|
| **Auth** | 인증, 세션, 역할 관리 | AuthContext, RoleSelectionPage |
| **Content** | 문서 CRUD, AI 생성, PDF/URL 처리 | MentorDashboard, DocsContext, PdfViewer, UrlPreviewPanel |
| **Learning** | 학습 진행, 퀴즈, 기록 | MenteeList, MenteeStudy |
| **AI** | Gemini/WebLLM 엔진 관리 | AIContext, AIEngineSelector, webllm.js |
| **Admin** | 사용자/문서 관리, 통계 | AdminDashboard |

---

## 3. 블럭 정의

### 3.1 Auth Domain Blocks

```
┌─────────────────────────────────────────────────────────────┐
│                     AUTH DOMAIN                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │    login     │───▶│   session    │───▶│    role      │  │
│  │    Block     │    │    Block     │    │    Block     │  │
│  └──────────────┘    └──────────────┘    └──────────────┘  │
│         │                   │                   │          │
│         ▼                   ▼                   ▼          │
│  • Google OAuth       • 세션 상태          • 역할 선택     │
│  • Email 로그인       • 프로필 조회        • 역할 변경     │
│  • 로그아웃           • 상태 구독          • 권한 검사     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| Block ID | 책임 | 입력 | 출력 |
|----------|------|------|------|
| `auth.login` | OAuth/Email 로그인 | `LoginRequest` | `AuthSession` |
| `auth.session` | 세션 상태 관리 | `AuthSession` | `UserProfile` |
| `auth.role` | 역할 선택/검증 | `UserProfile` | `RoleState` |

### 3.2 Content Create Agent Blocks (신규)

> **담당 에이전트**: `content-create-agent`
> **관련 파일**: MentorDashboard.jsx 전반부, PdfViewer.jsx, UrlPreviewPanel.jsx

```
┌─────────────────────────────────────────────────────────────┐
│                 CONTENT CREATE (콘텐츠 생성)                  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │    input     │───▶│   generate   │───▶│   preview    │  │
│  │    Block     │    │    Block     │    │    Block     │  │
│  └──────────────┘    └──────────────┘    └──────────────┘  │
│         │                   │                   │          │
│         ▼                   ▼                   ▼          │
│  • Text/URL/PDF 입력   • AI 섹션 생성      • 생성 결과 미리보기 │
│  • 소스 타입 선택      • AI 퀴즈 생성      • 퀴즈 검증 표시    │
│  • 텍스트 추출         • 에러 핸들링       • 수정 전 확인      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| Block ID | 책임 | 입력 | 출력 |
|----------|------|------|------|
| `content.input` | 소스 입력 처리 | `{ type: 'text'\|'url'\|'pdf', data }` | `ExtractedText` |
| `content.generate` | AI 콘텐츠 생성 | `{ text, title, options }` | `{ sections, quiz }` |
| `content.preview` | 생성 미리보기 | `GeneratedDoc` | React Component |

### 3.3 Content Manage Agent Blocks (신규)

> **담당 에이전트**: `content-manage-agent`
> **관련 파일**: DocsContext.jsx, MentorDashboard.jsx 후반부

```
┌─────────────────────────────────────────────────────────────┐
│                 CONTENT MANAGE (콘텐츠 관리)                  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────┐    ┌──────────────────────┐  │
│  │        document          │    │      quiz-mgmt       │  │
│  │         Block            │    │        Block         │  │
│  └──────────────────────────┘    └──────────────────────┘  │
│              │                            │                │
│              ▼                            ▼                │
│  • 문서 저장/수정/삭제            • 퀴즈 검증                │
│  • Supabase + Dexie 동기화        • 퀴즈 재생성 요청         │
│  • 내 문서 목록 관리              • 더미 퀴즈 필터링         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| Block ID | 책임 | 입력 | 출력 |
|----------|------|------|------|
| `content.document` | 문서 CRUD | `DocRequest` | `OJTDocument` |
| `content.quiz-mgmt` | 퀴즈 관리 | `{ quiz, action }` | `ValidatedQuiz` |

### 3.4 Learning Study Agent Blocks (신규)

> **담당 에이전트**: `learning-study-agent`
> **관련 파일**: MenteeList.jsx, MenteeStudy.jsx 전반부

```
┌─────────────────────────────────────────────────────────────┐
│                 LEARNING STUDY (학습 진행)                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────┐    ┌────────────────────────┐  │
│  │        roadmap         │    │        section         │  │
│  │         Block          │    │         Block          │  │
│  └────────────────────────┘    └────────────────────────┘  │
│              │                            │                │
│              ▼                            ▼                │
│  • 학습 로드맵 표시              • 섹션 순차 학습           │
│  • 팀별 문서 필터링              • 이전/다음 네비게이션     │
│  • 진도 현황 표시                • 학습 완료 처리          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| Block ID | 책임 | 입력 | 출력 |
|----------|------|------|------|
| `learning.roadmap` | 학습 로드맵 표시 | `UserId, Team` | `RoadmapView` |
| `learning.section` | 섹션 학습 진행 | `DocId, SectionIndex` | `SectionState` |

### 3.5 Learning Quiz Agent Blocks (신규)

> **담당 에이전트**: `learning-quiz-agent`
> **관련 파일**: MenteeStudy.jsx 후반부 (퀴즈 모드)

```
┌─────────────────────────────────────────────────────────────┐
│                 LEARNING QUIZ (퀴즈 응시)                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────┐    ┌────────────────────────┐  │
│  │          quiz          │    │        record          │  │
│  │         Block          │    │         Block          │  │
│  └────────────────────────┘    └────────────────────────┘  │
│              │                            │                │
│              ▼                            ▼                │
│  • 퀴즈 랜덤 출제                • 학습 기록 저장           │
│  • 정답 검증                    • 점수 계산/저장           │
│  • 결과 피드백                  • 통과/실패 판정           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| Block ID | 책임 | 입력 | 출력 |
|----------|------|------|------|
| `learning.quiz` | 퀴즈 응시 | `DocId, UserId` | `QuizSession` |
| `learning.record` | 학습 기록 저장 | `QuizResult` | `LearningRecord` |

### 3.6 AI Domain Blocks

```
┌─────────────────────────────────────────────────────────────┐
│                      AI DOMAIN                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   engine     │───▶│   gemini     │    │   webllm     │  │
│  │   selector   │    │    Block     │    │    Block     │  │
│  └──────────────┘    └──────────────┘    └──────────────┘  │
│         │                   │                   │          │
│         ▼                   ▼                   ▼          │
│  • 엔진 선택           • Gemini API        • WebLLM 로드  │
│  • 상태 표시           • Fallback 처리     • 브라우저 LLM │
│  • Fallback 관리       • API 호출          • 모델 관리    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| Block ID | 책임 | 입력 | 출력 |
|----------|------|------|------|
| `ai.engine` | 엔진 선택/관리 | `EngineConfig` | `AIEngine` |
| `ai.gemini` | Gemini API 호출 | `Prompt` | `AIResponse` |
| `ai.webllm` | WebLLM 실행 | `Prompt` | `AIResponse` |

### 3.7 Admin Domain Blocks

```
┌─────────────────────────────────────────────────────────────┐
│                     ADMIN DOMAIN                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │    users     │    │   contents   │    │    stats     │  │
│  │    Block     │    │    Block     │    │    Block     │  │
│  └──────────────┘    └──────────────┘    └──────────────┘  │
│         │                   │                   │          │
│         ▼                   ▼                   ▼          │
│  • 사용자 목록         • 문서 관리          • 통계 집계    │
│  • 역할 변경           • 전체 문서 조회     • 차트 표시    │
│  • 승인/거절           • 삭제               • 리포트       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| Block ID | 책임 | 입력 | 출력 |
|----------|------|------|------|
| `admin.users` | 사용자 관리 | `AdminQuery` | `UserList` |
| `admin.contents` | 전체 문서 관리 | `AdminQuery` | `DocList` |
| `admin.stats` | 통계 및 리포트 | `StatsQuery` | `Analytics` |

### 3.8 블럭 의존성 그래프 (v1.1.0)

```
                    ┌─────────────────┐
                    │   Orchestrator   │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
   ┌─────────┐         ┌─────────┐         ┌─────────┐
   │  Auth   │         │ Content │         │Learning │
   │ Domain  │         │ Domain  │         │ Domain  │
   └────┬────┘         └────┬────┘         └────┬────┘
        │                   │                   │
        │              ┌────┴────┐              │
        │              ▼         ▼              │
        │         ┌─────────┐ ┌─────────┐      │
        │         │   AI    │ │  Admin  │      │
        │         │ Domain  │ │ Domain  │      │
        │         └─────────┘ └─────────┘      │
        │                                       │
        └───────────────────────────────────────┘
                    (인증 필요)
```

**Cross-Domain Dependencies:**

| From | To | 이유 |
|------|-----|------|
| `content.generate` | `ai.gemini`, `ai.webllm` | AI 콘텐츠 생성 |
| `learning.*` | `auth.session` | 사용자 인증 필요 |
| `admin.*` | `auth.role` | Admin 권한 검사 |
| `content.document` | `auth.session` | 작성자 정보 |

---

## 4. 에이전트 시스템

### 4.1 에이전트 계층 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    AGENT HIERARCHY                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Level 0: Orchestrator                                       │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                  OrchestratorAgent                   │    │
│  │  • 전체 워크플로우 조정                              │    │
│  │  • 도메인 에이전트 라우팅                           │    │
│  │  • 글로벌 에러 핸들링                               │    │
│  └─────────────────────────────────────────────────────┘    │
│                            │                                 │
│  Level 1: Domain Agents    │                                 │
│  ┌─────────┬─────────┬─────────┬─────────┬─────────┐       │
│  │  Auth   │ Content │Learning │   AI    │  Admin  │       │
│  │  Agent  │  Agent  │  Agent  │  Agent  │  Agent  │       │
│  └────┬────┴────┬────┴────┬────┴────┬────┴────┬────┘       │
│       │         │         │         │         │             │
│  Level 2: Block Agents                                       │
│  ┌────┴───┐┌────┴───┐┌────┴───┐┌────┴───┐┌────┴───┐       │
│  │login   ││generate││roadmap ││engine  ││users   │       │
│  │session ││document││study   ││gemini  ││contents│       │
│  │role    ││quiz    ││record  ││webllm  ││stats   │       │
│  └────────┘└────────┘└────────┘└────────┘└────────┘       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 에이전트-블럭 매핑 테이블 (v1.1.0 - 7 Agents)

| Agent ID | Level | Managed Blocks | 주요 책임 | 예상 파일 수 |
|----------|-------|----------------|-----------|-------------|
| `orchestrator` | 0 | - | 전역 조정, 에러 처리 | - |
| `auth-agent` | 1 | `auth.*` (3개) | 인증/인가 전체 | 6-8개 |
| `content-create-agent` | 1 | `content.input`, `content.generate`, `content.preview` | 콘텐츠 생성 | 8-10개 |
| `content-manage-agent` | 1 | `content.document`, `content.quiz-mgmt` | 콘텐츠 관리 | 4-6개 |
| `learning-study-agent` | 1 | `learning.roadmap`, `learning.section` | 학습 진행 | 4-6개 |
| `learning-quiz-agent` | 1 | `learning.quiz`, `learning.record` | 퀴즈/기록 | 4-6개 |
| `ai-agent` | 1 | `ai.*` (3개) | AI 엔진 관리 | 6-8개 |
| `admin-agent` | 1 | `admin.*` (3개) | 시스템 관리 | 4-6개 |

**총 블럭 수**: 19개 (v1.0.0: 17개 → v1.1.0: 19개)

### 4.3 Conductor 워크플로우

개발자는 코더(Coder)가 아닌 **지휘자(Conductor)**가 됩니다:

```
┌─────────────────────────────────────────────────────────────┐
│                    CONDUCTOR WORKFLOW                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│    Developer (지휘자)                                        │
│         │                                                    │
│         ▼                                                    │
│    "Auth 도메인에서 소셜 로그인 추가해줘"                     │
│         │                                                    │
│         ▼                                                    │
│    Orchestrator Agent                                        │
│         │ 작업 분해 및 라우팅                                │
│         ▼                                                    │
│    Auth Domain Agent                                         │
│         │ features/auth/ 내에서 작업                         │
│         ▼                                                    │
│    auth.login Block Agent                                    │
│         │ AGENT_RULES.md 규칙 준수                           │
│         ▼                                                    │
│    결과 보고 + PR 생성 제안                                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**지휘 명령 예시 (v1.1.0):**

| 지휘 명령 | 라우팅 | 실행 블럭 |
|----------|--------|----------|
| "인증에 이메일 로그인 추가해줘" | `auth-agent` | auth.login |
| "PDF 텍스트 추출 개선해줘" | `content-create-agent` | content.input |
| "문서 저장 로직 수정해줘" | `content-manage-agent` | content.document |
| "학습 로드맵 UI 개선해줘" | `learning-study-agent` | learning.roadmap |
| "퀴즈 결과 화면 수정해줘" | `learning-quiz-agent` | learning.quiz |
| "WebLLM 모델 선택 UI 개선해줘" | `ai-agent` | ai.engine |

---

## 5. Vertical Slicing 구조

### 5.1 Before (현재 구조)

```
src-vite/src/
├── components/          # 모든 컴포넌트 (9개 혼재)
├── contexts/            # 모든 Context (4개 혼재)
├── utils/               # 모든 유틸 (5개 혼재)
└── constants.js

문제: AI가 "인증 기능"을 수정하려면 4개 폴더를 뒤져야 함
```

### 5.2 After (Vertical Slicing - v1.1.0)

```
src-vite/src/
├── features/                       # Feature-Based 모듈
│   │
│   ├── auth/                       # auth-agent 담당
│   │   ├── components/
│   │   │   └── RoleSelectionPage.jsx
│   │   ├── hooks/
│   │   │   └── useAuth.js
│   │   ├── contexts/
│   │   │   └── AuthContext.jsx
│   │   ├── index.js               # Public API
│   │   └── AGENT_RULES.md         # 에이전트 규칙
│   │
│   ├── content/
│   │   ├── create/                 # content-create-agent 담당 (신규)
│   │   │   ├── components/
│   │   │   │   ├── ContentInputPanel.jsx   # 입력 UI (분리됨)
│   │   │   │   ├── ContentPreview.jsx      # 생성 미리보기 (분리됨)
│   │   │   │   ├── PdfViewer.jsx
│   │   │   │   └── UrlPreviewPanel.jsx
│   │   │   ├── services/
│   │   │   │   ├── inputProcessor.js       # 소스 처리
│   │   │   │   └── contentGenerator.js     # AI 생성
│   │   │   ├── index.js
│   │   │   └── AGENT_RULES.md
│   │   │
│   │   └── manage/                 # content-manage-agent 담당 (신규)
│   │       ├── components/
│   │       │   ├── MyDocsList.jsx          # 내 문서 목록 (분리됨)
│   │       │   └── QuizManageModal.jsx     # 퀴즈 관리 (분리됨)
│   │       ├── hooks/
│   │       │   └── useDocs.js
│   │       ├── contexts/
│   │       │   └── DocsContext.jsx
│   │       ├── services/
│   │       │   └── quizValidator.js
│   │       ├── index.js
│   │       └── AGENT_RULES.md
│   │
│   ├── learning/
│   │   ├── study/                  # learning-study-agent 담당 (신규)
│   │   │   ├── components/
│   │   │   │   ├── MenteeList.jsx
│   │   │   │   └── SectionViewer.jsx       # 섹션 학습 (분리됨)
│   │   │   ├── hooks/
│   │   │   │   └── useLearningProgress.js
│   │   │   ├── index.js
│   │   │   └── AGENT_RULES.md
│   │   │
│   │   └── quiz/                   # learning-quiz-agent 담당 (신규)
│   │       ├── components/
│   │       │   ├── QuizSession.jsx         # 퀴즈 응시 (분리됨)
│   │       │   └── QuizResult.jsx          # 결과 표시 (분리됨)
│   │       ├── hooks/
│   │       │   └── useLearningRecord.js
│   │       ├── index.js
│   │       └── AGENT_RULES.md
│   │
│   ├── ai/                         # ai-agent 담당
│   │   ├── components/
│   │   │   └── AIEngineSelector.jsx
│   │   ├── contexts/
│   │   │   └── AIContext.jsx
│   │   ├── services/
│   │   │   ├── geminiService.js
│   │   │   └── webllmService.js
│   │   ├── index.js
│   │   └── AGENT_RULES.md
│   │
│   └── admin/                      # admin-agent 담당
│       ├── components/
│       │   ├── AdminDashboard.jsx
│       │   ├── UserManagement.jsx          # 사용자 관리 (분리됨)
│       │   └── StatsCharts.jsx             # 통계 차트 (분리됨)
│       ├── hooks/
│       │   └── useAdmin.js
│       ├── index.js
│       └── AGENT_RULES.md
│
├── shared/                         # 공유 유틸리티 (최소화)
│   ├── ui/
│   │   ├── Header.jsx
│   │   ├── Spinner.jsx
│   │   └── EmptyState.jsx
│   ├── contexts/
│   │   └── ToastContext.jsx
│   └── utils/
│       ├── supabaseClient.js
│       ├── db.js
│       ├── cors-proxy.js
│       └── helpers.js
│
├── App.jsx
├── main.jsx
└── constants.js
```

> **v1.1.0 핵심 변경**: `content/`와 `learning/`이 각각 2개 하위 폴더로 분리됨

### 5.3 블럭 사이즈 가이드 (v1.1.0)

| 에이전트 | 폴더 | 예상 파일 수 | 예상 토큰 | 상태 |
|----------|------|-------------|----------|------|
| `auth-agent` | `features/auth/` | 6개 | ~15k | 🟢 적정 |
| `content-create-agent` | `features/content/create/` | 8개 | ~25k | 🟢 적정 |
| `content-manage-agent` | `features/content/manage/` | 6개 | ~20k | 🟢 적정 |
| `learning-study-agent` | `features/learning/study/` | 5개 | ~12k | 🟢 적정 |
| `learning-quiz-agent` | `features/learning/quiz/` | 5개 | ~15k | 🟢 적정 |
| `ai-agent` | `features/ai/` | 6개 | ~20k | 🟢 적정 |
| `admin-agent` | `features/admin/` | 6개 | ~18k | 🟢 적정 |
| (공유) | `shared/` | 8개 | ~15k | 🟢 적정 |

**v1.0.0 대비 개선:**

| 항목 | v1.0.0 | v1.1.0 |
|------|--------|--------|
| 최대 에이전트 파일 수 | 12개 (content) | 8개 |
| 최대 에이전트 토큰 | ~35k (content) | ~25k |
| 평균 에이전트 파일 수 | 8개 | 6개 |

**기준:**
- 🟢 적정: 8개 이하 파일, 25k 토큰 이하
- 🟡 모니터링: 10개+ 파일, AI 응답 10초+
- 🔴 분할 필요: 15개+ 파일

---

## 6. Agent Rules

### 6.1 파일 구조 (v1.1.0 - 7 Agents)

```
.claude/
├── agents/
│   ├── orchestrator.md           # 최상위 오케스트레이터 규칙
│   ├── auth-agent.md             # Auth 에이전트 규칙
│   ├── content-create-agent.md   # Content Create 에이전트 규칙 (신규)
│   ├── content-manage-agent.md   # Content Manage 에이전트 규칙 (신규)
│   ├── learning-study-agent.md   # Learning Study 에이전트 규칙 (신규)
│   ├── learning-quiz-agent.md    # Learning Quiz 에이전트 규칙 (신규)
│   ├── ai-agent.md               # AI 에이전트 규칙
│   └── admin-agent.md            # Admin 에이전트 규칙

src-vite/src/features/
├── auth/AGENT_RULES.md
├── content/
│   ├── create/AGENT_RULES.md     # 콘텐츠 생성 전용 (신규)
│   └── manage/AGENT_RULES.md     # 콘텐츠 관리 전용 (신규)
├── learning/
│   ├── study/AGENT_RULES.md      # 학습 진행 전용 (신규)
│   └── quiz/AGENT_RULES.md       # 퀴즈 응시 전용 (신규)
├── ai/AGENT_RULES.md
└── admin/AGENT_RULES.md
```

### 6.2 Agent Rules 템플릿

```markdown
<!-- features/auth/AGENT_RULES.md -->

# Auth Block Agent Rules

## Identity
- **Role**: 인증/인가 전문가
- **Domain**: Auth
- **Scope**: `features/auth/` 내부만

## Constraints (제약 사항)

### DO (해야 할 것)
- ✅ 이 폴더 내 파일만 수정
- ✅ `index.js`를 통해 외부 노출 API 관리
- ✅ Supabase Auth API 사용
- ✅ 에러 처리 및 로깅 포함

### DON'T (하지 말 것)
- ❌ `features/` 외부 파일 직접 수정
- ❌ `shared/` 컴포넌트 내부 수정
- ❌ 다른 도메인 Context 직접 접근
- ❌ 하드코딩된 토큰/비밀값 사용

## Dependencies (의존성)

### 내부 의존성
- `shared/utils/supabaseClient.js`: Supabase 클라이언트

### 외부 의존성
- `@supabase/supabase-js`: 인증 API

## Testing
- 단위 테스트: `__tests__/` 폴더
- Mock: `supabase.auth` 함수만 Mock 허용

## Error Codes
- `AUTH_INVALID_CREDENTIALS`: 잘못된 인증 정보
- `AUTH_SESSION_EXPIRED`: 세션 만료
- `AUTH_ROLE_UNAUTHORIZED`: 권한 없음
```

---

## 7. 구현 로드맵

### 7.1 Phase 1: 구조 전환 (1주)

| Task | Priority | 설명 |
|------|----------|------|
| `features/` 디렉토리 생성 | P0 | 5개 도메인 폴더 생성 |
| 파일 이동 | P0 | 기존 파일을 도메인별로 분류 |
| `index.js` Public API 정의 | P0 | 각 도메인 외부 인터페이스 |
| Import 경로 수정 | P0 | 새 구조에 맞게 업데이트 |

### 7.2 Phase 2: Agent Rules 작성 (3일)

| Task | Priority | 설명 |
|------|----------|------|
| `.claude/agents/` 생성 | P1 | 도메인 에이전트 규칙 |
| `AGENT_RULES.md` 작성 | P1 | 각 블럭별 규칙 파일 |
| Claude 설정 업데이트 | P1 | CLAUDE.md에 블럭 구조 반영 |

### 7.3 Phase 3: 테스트 및 검증 (3일)

| Task | Priority | 설명 |
|------|----------|------|
| 빌드 테스트 | P0 | `npm run build` 통과 |
| E2E 테스트 | P1 | 기존 기능 동작 확인 |
| AI 컨텍스트 테스트 | P1 | 블럭별 AI 응답 속도 측정 |

---

## 변경 이력

| Version | Date | Changes |
|---------|------|---------|
| 1.0.0 | 2025-12-09 | 초기 아키텍처 문서 (5 Domain Agents) |
| 1.1.0 | 2025-12-09 | 코드 복잡도 분석 기반 7 Agents 재설계 |

---

## 참조

### 내부 문서

- [PRD v3.0](../../tasks/prds/v3/00-master-prd.md)
- [Database Schema](../../tasks/prds/v3/04-database/04-01-schema.md)

### 외부 참조

- [WSOPTV Block Agent System](d:/AI/claude01/wsoptv/docs/architecture/0001-block-agent-system.md)
- [Vertical Slice Architecture](https://www.milanjovanovic.tech/blog/vertical-slice-architecture)
