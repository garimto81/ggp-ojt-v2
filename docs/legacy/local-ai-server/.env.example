# OJT Master - 사내 Docker 배포 환경 설정
# 이 파일을 .env로 복사하여 사용하세요

# ============================================
# 필수 설정
# ============================================

# Supabase 연결 정보 (Supabase 대시보드 > Project Settings > API)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# 외부 Local AI 서버 URL (이미 실행 중인 vLLM 서버)
VITE_LOCAL_AI_URL=http://10.10.100.209:8001

# ============================================
# 인증 설정 (Issue #105, #107)
# ============================================

# 인증 모드 - Docker 사내 배포에서는 반드시 'email' 사용!
# - hybrid: Google OAuth + Email (Vercel 프로덕션용)
# - email: Email 인증만 (Docker 사내 배포 필수)
# - google: Google OAuth만
# ⚠️ 로컬 네트워크(http://10.x.x.x)에서 Google OAuth는 동작하지 않습니다!
VITE_AUTH_MODE=email

# ============================================
# 선택 설정
# ============================================

# 앱 포트 (기본: 3000, 80 사용 중이면 다른 포트 지정)
APP_PORT=3000

# Cloudflare R2 이미지 업로드 (선택)
VITE_R2_WORKER_URL=
